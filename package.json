{
  "name": "foldlab",
  "license": "Apache-2.0",
  "author": "Mepuka Kessy (https://github.com/mepuka)",
  "private": true,
  "type": "module",
  "description": "Effect v4 + Go lab: stream folds, chain identity, and the commitment algebra.",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "effect": "4.0.0-rc.108",
      "@effect/platform-bun": "4.0.0-rc.108"
    }
  },
  "scripts": {
    "prepare": "bun scripts/prepare-effect-language-service.ts",
    "typecheck": "bun scripts/prepare-effect-language-service.ts --require && tsgo -p packages/core/tsconfig.json --noEmit && tsgo -p packages/moves/tsconfig.json --noEmit && tsgo -p packages/plait/tsconfig.json --noEmit && tsgo --noEmit && effect-language-service diagnostics --project packages/plait/tsconfig.json --strict",
    "test": "bun test",
    "test:packages": "bun run --filter './packages/*' test",
    "gates": "bun scripts/gates.ts"
  },
  "dependencies": {
    "@foldlab/core": "workspace:*"
  },
  "devDependencies": {
    "@effect/language-service": "0.87.2",
    "@types/bun": "1.3.14",
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "bun-types": "1.3.14",
    "typescript": "^5.9.2"
  }
}
