# FINDINGS — the program's append-only findings ledger

Schema and immutability rules: PROCEDURE.md §6. Rows append; a disposition cell is
updated exactly once, from OPEN to its resolution. Seeded 2026-08-25 with day one's
findings, in rough order of discovery.

| # | Date | Source | Finding | Receipt | Disposition |
|---|---|---|---|---|---|
| F-1 | 2026-08-25 | decode seat | fixed-width be64 frames truncate, falsifying encode injectivity | `E2/Encode.lean` header | Q10 amendment: unbounded LEB128 `encNat` |
| F-2 | 2026-08-25 | coordinator (pinning) | L-faithful is false over raw maps — a raw binding can fail `getChecked`'s hash check | STORE-MODEL §3 "reachable states" | M15 pinned over `Reachable` + hypothesis-free fresh half |
| F-3 | 2026-08-25 | coordinator (pinning) | spec-to-scaffold gap: §5 demanded `canonV`, `preimageE` embedded values as given — L-dedup silently failed for entities | STORE-MODEL §7 Q11 record | Q11: `canonV`, canonicalizing `preimageE`, M12E proved |
| F-4 | 2026-08-25 | scout B | Effect's codec path reorders union members (`makeReorder`), so an address at `ast` differs from one at `toCodecJsonAST(ast)` | B §census-gap; Schema.ts:15390 | G6: address subject is the AST as constructed |
| F-5 | 2026-08-25 | scout B | R-4 relabelling hole: annotations spread last + `Filter.annotate` + id dispatch lets a check claim to be a predicate it does not enact | B report, three pinned receipts | R-4 deferred to a dedicated session (G7); B's 75-site catalog is its input |
| F-6 | 2026-08-25 | scout B | `Json`/`MutableJson` are one `Declaration` differing only by an annotation string — annotation-insensitive addresses collide them | SchemaAST.ts:4352-4376 | informs `Declaration` REJECTED-v1 (MAPPING row 8); annotation identity → R-4 session |
| F-7 | 2026-08-25 | scout A | the natural mapping sends 21 Effect variants to 14 addresses; every collision is a pre-image byte equality (hash-independent) | A `p07_rollup` verbatim | G1 minted check-ids; MAPPING dispositions |
| F-8 | 2026-08-25 | scout A | `Value` has no float: `Schema.Number` cannot type 1.5; propagates to check payloads | A §3; Core.lean "no float" | G2: floats REJECTED-v1, explicitly; no silent int reinterpretation |
| F-9 | 2026-08-25 | scout A | the `mu` discriminator in the pre-image splits alpha-equivalent recursive schemas — the inverse of Unison's design | A `p05` | G3: discriminator stays in identity; alpha-invariance a recorded non-goal |
| F-10 | 2026-08-25 | scout A | tuple-with-rest and index signatures are inexpressible; the workarounds are wrong at the value plane | A `p03` `flat_rejected`, `object_exact_width` | G4 → A-4 landed (tags 0x3B/0x3C); MAPPING rows 4/18 MAPS |
| F-11 | 2026-08-25 | scout A | `.lit` is strictly wider than Effect's `LiteralValue` — the carrier admits schemas no source produces | A §3 dual | MAPPING admission rule 1 (`.lit` narrowing) |
| F-12 | 2026-08-25 | scout C | canon is an involution, not idempotent, on duplicate-key runs — both unconditional S1 obligations FALSE; the schemas are reachable because `WFS` lacks §5 clause 4 | `probe3_ties` (kernel-checked, coordinator-reverified) | S1 restated conditionally; A-3 implemented; M12/M15 unaffected |
| F-13 | 2026-08-25 | scout C | §16 provenance-merge and §17 naming-convergence die on array order-sensitivity; the `vobj`-key workaround provably loses WF2 coverage | C probes 2/4 | G5: no addresses in keys; set/map nodes = candidate A-5; claims demoted pending-A-5 |
| F-14 | 2026-08-25 | scout C | git delivers a SET of pre-images; `Reachable` is sequential; no theorem bridges them | C transport gap | M19 stated owed; R-15c holds no G1 claim until proved |
| F-15 | 2026-08-25 | scout C | L-comm is false as `StoreMap` value equality, true up to `find` | C probe 5 | M11-commutation must pin up to `find`-extensionality (STORE-MODEL §7 note) |
| F-16 | 2026-08-25 | A-4 seat | A-4 breaks the shell's exhaustive `renderSchema` match; the parser falls through silently | seat report finding 1 | closed at adjudication `e9b1bcc`: render/parse arms, gate round-trips, script 10 |
| F-17 | 2026-08-25 | A-4 seat | `Correspondence` part (1) never gained an `address` ascription at A-1 | seat report finding 2 | closed at `e9b1bcc` |
| F-18 | 2026-08-25 | A-4 seat | byte-literal equalities through the mutual encoder yield to neither `decide` nor `rfl`; `simp` with the named equation lemmas works | seat report finding 3 | house lesson; carries into the next brief |
| F-19 | 2026-08-25 | A-4 seat | `tupleRest` conformance is syntax-directed — `ConformsL` is lockstep, the split point is forced | seat report finding 5 | pinned in STORE-MODEL §7 for the M18 seat |
| F-20 | 2026-08-25 | coordinator | the no-push posture had no mechanism: `origin/main` advanced and a codex branch reached the remote (pusher UNVERIFIED) | audit day-one §5.2 | push guard hook installed (audit §6.2); remote branch deleted by operator ruling |
| F-21 | 2026-08-25 | coordinator (adjudication) | SHELL-v0 predates A-3: the boundary does not name `dupFreeS`; operationally covered by the canonicity byte-compare | STORE-SHELL §9 | OPEN — follow-up owed: boundary names `dupFreeS` explicitly |
| F-22 | 2026-08-25 | coordinator (audit) | spec-to-scaffold transcription is untracked — two same-day instances (F-3, F-12) | audit day-one §5.1 | clause-conformance table ratified as promotion requirement (audit §6.1) |
| F-23 | 2026-08-25 | coordinator (pinning B4) | `canonS` passes `lit` payloads through while `canonV` sorts the same bytes — the unconditional conformance bridge is false; one carrier, two canonical forms | probe: `canonS (.lit unsorted) == .lit unsorted` → `true`, `canonV unsorted == unsorted` → `false` | B4 pinned conditionally on `litsCanonicalB`; Q13 RULED YES 2026-08-25 → amendment A-6, scheduled for the post-wave-2 window |
| F-24 | 2026-08-25 | coordinator (pinning B4) | the bridge's `refine` case requires `checkSem` invariance under `canonV` — order-insensitivity is an admission criterion for checks | B4's env hypothesis, `E2/Bridge.lean` | routed to the R-4 dedicated session as an allowlist admission criterion |
| F-25 | 2026-08-25 | refuter 1 | M17 (typed reachability) is FALSE on today's Reachable: putE checks Conforms on raw carriers, stores canonical forms; the refine/checkSem route has no lit node so A-6 does not close it | R1 `M17_store_form_FALSE`, `M17_survives_A6_FALSE` (kernel, reverified) | RULED W3-7 (2026-08-25): Reachable premises restated on stored forms (i-a); B4 retired; M17 seat after window B |
| F-26 | 2026-08-25 | refuter 1 | A-6 as ruled RE-FALSIFIES S1 (dupFreeS (.lit _) = true unconditionally, so canon involution reappears one plane up); repair dupFreeS (.lit v) := dupFreeV v, verified over 15,310 schemas | R1 `A6_refalsifies_S1` (kernel, reverified) | RULED W3-10 (2026-08-25): ships with A-6 in window A; seat dispatched same day |
| F-27 | 2026-08-25 | refuter 1 | A-4 edges collapse: `.array e ≡ .tupleRest .nil e` for every e (infinite L-2787 family), `.tuple es ≡ .tupleRest es Never`, `.record Never ≡ .object .nil` | R1 `A2_two_addresses`, `B3_tuple_eq_tupleRest_never`, `C1` (kernel) | RULED W3-17 (2026-08-25): `canonicalSpellingB` as `WFS` conjuncts, window B; carrier narrowing reserved for R-2 |
| F-28 | 2026-08-25 | refuter 1 | duplicate-key VALUES are reachable (via .record and via .lit), so ObligationCanonVIdempotent is vacuous exactly where F-12 bit; A-3's value-plane boundary admission is unenforced | R1 `E5_dup_value_reachable`, `E6` (kernel) | RULED W3-9 (2026-08-25): A-3 record reversed — `dupFreeV` premises on `Reachable` (window B) plus the F-26 clause; check payloads stay F-29/R-4 |
| F-29 | 2026-08-25 | refuter 1 | check payloads are address-significant and nothing canonicalizes them (F-24's twin): two source-identical refinements take two addresses | R1 `C5`/`C6`/`C7` via encSchema_inj | OPEN — R-4 session input |
| F-30 | 2026-08-25 | refuter 3 | M19 as worded (G8) REFUTED four ways: naive ref-closure ignores referent KIND, conformance, WFS; "exactly that set" fails under H-collision; repair = re-base onto candidate stores (Admissible structure supplied) | R3 `C_obstruction`, `D_no_conformance`, `F_exactly_fails` (kernel) | RULED W3-22 (2026-08-25): pinned re-based on `Admissible`; pin seat owed |
| F-31 | 2026-08-25 | refuter 3 | M10 SURVIVES for arbitrary H (putPre no-ops on occupied address); statement must say nodes are ADDRESSES not pre-images; ObligationM10_rank (store list is reverse topological order) proposed | R3 `R3-p1_graph` survivals | disposition: pin M10 with the address-node vocabulary |
| F-32 | 2026-08-25 | refuter 3 | SH5 FALSE as worded: a WF1+WF2-clean store can contain a reference cycle and be unreachable; acyclicity is independent and unchecked (Kahn's algorithm both decides it and emits M19's witness) | R3 `HEADLINE_wf1_wf2_insufficient` (kernel) | RULED W3-12 (2026-08-25): Kahn's `topoOrder` decides WF3 and emits the order; seat owed |
| F-33 | 2026-08-25 | refuters 2+3 | the shell boundary enforces NO part of WFS (not dupFreeS/closedB/guardedB); `.var 0` and `(mu d (var 0))` store and `check` clean, so `check clean` does not imply Reachable — contradicting STORE-SHELL §4 | R3 `HEADLINE_scan_does_not_establish_reachability`; R2 target 2 | RULED W3-12/W3-13 (2026-08-25): boundary and scan call the well-formedness decision, ordered before canonicity; SH5 narrows to `Admissible`; seat owed |
| F-34 | 2026-08-25 | refuter 3 | MAPPING's single-spelling rule names 2 constructs; there are ≥10 spelling families, one unbounded (`.mu d X` for binder-free X — one address per discriminator) | R3 `R3-p3_spellings` (10 proved iffs + byte receipts) | RULED W3-17 (2026-08-25), with F-27; the rule's completeness carries an unchecked-claim marker |
| F-35 | 2026-08-25 | refuter 3 | `.lit (.vaddr a)` hides an address from refsS entirely, so MAPPING admission rule 1 (.lit narrowing) is LOAD-BEARING for WF2 — the model owes a clause | R3 SP-11 `sp11_reachable` (kernel) | RULED W3-18 (2026-08-25): `litNarrowB` as a `WFS` conjunct, window B |
| F-36 | 2026-08-25 | refuter 3 | Q12's price is REAL not theoretical: under oneOf a second success is a decode failure (census §5a, SchemaAST.ts:3071-3073), so M17 would certify a value Effect's decoder rejects; recommend naming the price in M17's anti-claim rather than paying it | R3 `ov_reachable`, `mode_is_in_identity` (kernel) | RULED W3-11 (2026-08-25): price named in M17's anti-claim; posture (b) and `anyOfOnly` rejected |
| F-37 | 2026-08-25 | refuter 3 | R-15c: git's connectivity covers git's DAG; our ref edges live in blob content git never parses, contributing zero to WF2/WF3; hazards: core.autocrlf on binary pre-images (.gitattributes fix), 128-hex names vs Windows MAX_PATH, checkout deletes objects (append-only is not a git property) | R3 §5.2 (UNVERIFIED dual-host hazards flagged) | RULED W3-12/W3-23 (2026-08-25): rider adopted; `.gitattributes` discipline + `init` behavior seat owed; Windows items stay UNVERIFIED pending the dual-host leg |
| F-38 | 2026-08-25 | refuter 3 | F-15: listing order is NOT observable (shell sorts by address, Boundary.lean:196-207) but find-equality is coarser than check's printed objects= count; companion lemma reachable_keys_nodup proved (key-functionality is a Reachable invariant) | R3 `find_extensional`, `reachable_keys_nodup` (kernel) | disposition: M11-comm pins up to find-ext WITH reachable_keys_nodup |
| F-39 | 2026-08-25 | refuter 2 | DIFFERENTIAL DIVERGENCE: names plane is case-sensitive in the model, case-insensitive on disk (APFS/NTFS); name-set "Widget"/"widget" gives 2 model bindings, 1 disk file; name-get returns different addresses each side, both exit 0 — silent wrong answer | R2 target 1 (reproduction transcript) | RULED W3-14 (2026-08-25): host-relation neutrality minted; model authoritative; hex-encoded name files + 64-char cap + listing verb; seat owed |
| F-40 | 2026-08-25 | refuter 2 | F-21 EXPLODES: canonFields is an involution on duplicate-key runs, so a PALINDROMIC run byte-compares equal to its re-canon; a 3-field "a"-keyed carrier with 2 schemas is admitted and check-clean (same for vobj) | R2 target 2 (reproduction) | supersedes F-21; folds into ruling family 2 |
| F-41 | 2026-08-25 | refuter 2 | mirror defect: the boundary REJECTS bytes it produced itself — schema-put of a non-palindromic duplicate-key carrier is rejected non-canonical because preimageS is not idempotent as a byte function | R2 target 2 | RULED W3-13 (2026-08-25): ordering fix (well-formedness before canonicity, both sides); W3-19's flip retires the mechanism after the boundary check is live |
| F-42 | 2026-08-25 | refuter 2 | verification-on-open: a missing object/name file → uncaught Lean exception, exit 1 = "violations found" (verdict ambiguity, breaks §5); a FIFO → unbounded hang; readView cannot do better — the §3 whitelist has no file-type primitive | R2 target 3 (reproduction) | RULED W3-15 (2026-08-25): `symlinkMetadata` admitted single-purpose; `StoreFault` + three-way exit codes; G-S5; seat dispatched same day |
| F-43 | 2026-08-25 | refuter 2 (source-confirmed) | THREE G-S gate holes: (a) executable roots (Main/EncodeMain/HarnessMain) are outside the Shell-prefix scan — clock/getEnv/rand build all-gates-green; (b) a leading underscore evades G-S1/S2/S4 (Gate.lean:78); (c) G-S4 scans only E2, so the digest sha3_512 is shadowable | R2 target 4 + coordinator source check (Gate.lean:78/92/116) | FIXED `2f33ae0` (2026-08-25): coverage by module membership, enumerated companions, G-S4 over the digest namespaces; negatively validated then reverted |
| F-44 | 2026-08-25 | refuter 2 | H (sha3_512) runs ~26 KB/s; every verb re-opens with the full scan, so one 2 MB object costs 76 s on every subsequent verb forever (claims-free; v0 correctness only, but a v1 concern) | R2 target 5 | disposition: noted for v1; verification-on-open amortization |
| F-45 | 2026-08-25 | refuter 2 | bookkeeping: STORE-SHELL §9 and the shell README say "nine committed scripts"; there are ten (the harness agrees) | R2 target 5 note | disposition: fix the count; flag for ledger-extractor seat |
| F-46 | 2026-08-25 | wave-3 reader R-D | STORE-MODEL §5's typing precondition binds `s` to the resolved schema (which is `canonS s` by L-faithful); `Model.lean:310-311` transcribes the premise onto the raw carrier — an F-22-class spec-to-scaffold divergence | R-D §1.1 (`STORE-MODEL.md:136-139` vs `E2/Model.lean:310-312`) | RULED W3-8: the schema half of the window-B amendment is a transcription correction |
| F-47 | 2026-08-25 | wave-3 reader R-E | the fips202 absorb fold re-drops the message per block (`Impl.lean:96`), an accidental Θ(n²): ×4.01–4.17 per doubling, ≈half the 2 MB digest cost | R-E measurements, isolated | RULED W3-25: representation-preserving fix dispatched 2026-08-25; bridge + KATs must rebuild green |
| F-48 | 2026-08-25 | wave-3 reader R-E | F-12's involution root is one comparison: `insertField` places equal keys after the run (`Canon.lean:31`); flipping it makes `canonFields` a stable, idempotent sort (verified over all 243 duplicate-heavy length-5 lists; provable from core mergeSort lemmas) — but alone it makes F-40 strictly worse | R-E P10/P11 | RULED W3-19: the flip lands after the boundary well-formedness check is live |
| F-49 | 2026-08-25 | wave-3 reader R-E | `Std.TreeMap`/`ExtTreeMap` are kernel-rejected in nested-inductive position and `TreeMap.Raw` provides no guarantee — canonical-by-construction carriers via Std are unavailable; Std's map lemmas remain usable at the shell | R-E P1/P2/P4/P5 (`Std/Data/TreeMap/Basic.lean:63`) | negative result pinned; the subtype-carrier nested-position probe is the named follow-up |
| F-50 | 2026-08-25 | wave-3 reader R-B | union-member dedup is mode-semantic: under `oneOf`, `[X,X]` decodes nothing that `[X]` accepts while `Conforms` treats them as identical — a Q12 instance the wave-2 register did not record | R-B §2.3 (census §5a, `SchemaAST.ts:3071-3073`); probe owed | RULED into W3-17: any dedup clause is mode-gated |
| F-51 | 2026-08-25 | wave-3 reader R-B | MAPPING has no row for Effect `Objects` carrying property AND index signatures simultaneously; the current carrier cannot express `{ a: string, [k: string]: number }` | R-B §6.3 (census fields vs MAPPING rows 16/18) | OPEN — R-2 window input (carrier-narrowing companion) |
| F-52 | 2026-08-25 | boundary-wiring seat | the pinned toolchain has no symlink- or FIFO-creation primitive (`symlinkMetadata` only reads), so `place-symlink`/`place-fifo` are unwritable without a process-spawn capability — two of W3-15's four hazard legs stay hand-exercised, never scripted | seat report at `af13824`; shell README owed section | RULED CV-2 (2026-08-25): residue accepted; no spawn class admitted; re-verify at the dual-host exercise |
| F-53 | 2026-08-25 | coordinator (mise wiring) | the harness's `<fresh-work-dir>` precondition is UNCHECKED: given a stale workdir it runs and reports 10/26 divergences instead of refusing — a misreport, not a verdict (the verdict/environment-fault rule applied to the instrument itself) | gen:vectors first wiring, reproduced and isolated at `97944fa`'s follow-up | mise guards with `git clean` before each run; harness-side refusal (exit 2 on non-empty workdir) owed to a shell seat |
