# Tool register

A tool whose output flows into a gated artifact is admitted here first, with
its role and trust statement (modeled on the Rust std-lib verification
project's approved-tools rule). Start light; grow as work gets defined.
("Register", not "ledger" — the artifact-kind ledger is
[KINDS.md](KINDS.md).)

| Tool | Role | Trust statement |
| --- | --- | --- |
| Git client (version recorded per resolution receipt) | Resolve external repository selectors to full commit, tree, and blob identities and materialize exact blob bytes for Source Provenance | Resolution observation only. Full object identities, byte length, and an independent SHA-256 content digest must agree in the receipt; Git contributes no semantic or authorship claim. |
| PowerShell/.NET HTTP and SHA-256 surfaces (versions recorded per resolution receipt) | Fetch a versioned standards artifact as bytes and compute its external integrity digest | Transport and digest observation only. The receipt records the exact URL, response metadata, byte length, and digest; neither HTTPS nor SHA-256 establishes authorship, endorsement, or semantic correctness. |
| mise | Toolchain and task runner | Trusted for reproducing the declared environment; contributes nothing to claims. |
| bun | Dev runtime and package manager | Reproduces the declared dependency environment from the committed lockfile; contributes nothing to claims. |
| vitest + `@effect/vitest` (exact-pinned: `vitest@4.1.11`, `@effect/vitest@4.0.0-rc.112` — the `@effect/vitest` version equals the pinned effect rc by law; versions recorded 2026-08-29) | Effect-native test harness for the effects package | Test results are sampled evidence only (G4 at best); never a proof. |
| node (pinned) | Claim-target JavaScript engine (L0) | Named host for any future hosted-execution claim (G6). |
| elan / Lake / Lean 4 | Proof toolchain and referee | The kernel is the trust anchor for G1 claims; axiom reports required. |
| `leanchecker` (toolchain-pinned) | Fresh-environment replay of compiled Lean modules | Uses the same Lean kernel, so it detects environment manipulation but contributes no independent kernel assumption. |
| Lean4Lean (revision-pinned per run) | Separate replay of promoted Lean declarations | A separately implemented checker with shared ancestry noted by its authors; corroborating assurance only, never a replacement for the pinned Lean kernel. |
| `lean-action` v1 | Cross-host Lean setup for the canonical CI task | Build and cache orchestration only; contributes nothing to theorem claims. |
| LLM harnesses | Proof search, drafting, review | Empty trust contribution: every output must pass a machine-checked gate; the gate carries the trust. |
| Effects conformance-lane harness (LLM) | Authors the effects Lean model, schema-bundle instances (statement, sentence, anti-vacuity kit), declared mutants, and the manifest generator — under ratified statement schemas only ([CONFORMANCE-WORKFLOW](../../library/effects/CONFORMANCE-WORKFLOW.md)) | Empty trust contribution: the kernel carries proofs, ratified schemas carry statement shape, the gate carries generation determinism and kit presence. Never edits the TypeScript tree or a manifest by hand. |
| Effects implementation-lane harness (LLM) | Authors the effects TypeScript implementation and mirrored TS mutants until the gate is green against the last ratified manifest ([CONFORMANCE-WORKFLOW](../../library/effects/CONFORMANCE-WORKFLOW.md)) | Empty trust contribution: manifest agreement and the gate carry the evidence. Consumes ratified manifests only; never edits the Lean tree. |
| opam (annex-pinned) | Package manager for the [Coq/OCaml annex](../../annex/coq/README.md) | Trusted for reproducing the annex's declared switch from its committed export; contributes nothing to claims. |
| Rocq / Coq (annex) | Reading and running executable prior art | Evidence and technique only. A Rocq development is never an estate artifact, no gate G0–G6 may be stamped on one, and a technique read there enters the estate only by being restated and reproved in Lean. |
| TypeScript compiler API (`typescript@5.9.2` exactly, npm; classic JS API, not the 7.x native port) | Interim Stage 1 extractor instrument: syntax-only parse (`createSourceFile`, no checker) of the pinned Effect sources ([experiments/entity-store-extract](../../experiments/entity-store-extract/)) | Trusted only that its parser maps the pinned bytes to a faithful syntax tree; no type resolution is used, so no inference is trusted. Output enters gated work solely as `inventory.json`, which must pass the five-way enumeration agreement and the pinned-byte pre-check. Version drift is a re-admission event. |
| Lean `Std` IO surface (toolchain-internal: `Std.Http`, `Std.Async`, `Std.Sync`, `Std.Do`; extern/libuv-backed) | Shell-layer runtime for the executable store ([STORE-SHELL](../entity-store/STORE-SHELL.md), ruling SH8) | Same pin and trust unit as the already-admitted Lean toolchain — no separate instrument. The kernel-checked story covers the pure core only; shell namespaces sit deliberately OUTSIDE the E2 opaque/unsafe gate, and no G-grade claim attaches to `Std`'s IO behavior. `Std.Do` triples are ordinary kernel-checked theorems about shell code and carry trust like any other. |

| LeanServer (`AfonsoBitoque/LeanServer`, revision-pinned per the transport-standards receipt; formally adopted 2026-08-27, clone materialized and blob-verified per the [adoption record](../../library/effects/research/leanserver-adoption.md)) | First-class differential conformance PEER for remote-transport suites and the adopted substrate for real server semantics: an independent executable Lean networking stack run beside the effects adapter, differences adjudicated against the pinned standards and the Lean model ([remote-transport survey](../../library/effects/research/remote-transport-standards-and-lean-models.md)) | A peer under test, never a standards oracle: its audited gaps (gRPC trailers, HTTP/2 completion, exact message consumption, WebSocket masking/fragmentation) are named detection targets, and agreement with it proves nothing alone. Its own theorem claims carry no estate grade. Conformance suites bind to an abstract peer interface — LeanServer is one binding — so suites stay isolated from any single peer. Runs under its own pinned toolchain as an external tool, never as an in-tree Lake dependency. |

| lean4-tree-sitter (`predictable-machines/lean4-tree-sitter` @ `3a57f55e`, v0.2.4, Lean v4.32.0; C seam: vendored tree-sitter core v0.24.7 + `tree-sitter-typescript` grammar `75b3874e` compiled into one static archive — source and per-host build digests in the [stand-up receipt](../../.reference/provenance/receipts/lean4-tree-sitter-stage1-standup.json); admitted 2026-08-28) | Second Stage 1 extractor instrument: syntax-only tree-sitter walk over the pinned Effect sources ([experiments/entity-store-extract/twin](../../experiments/entity-store-extract/twin/)), run beside the TypeScript compiler API so the two extractors are each other's check | Trusted only that the vendored grammar's parse of the pinned bytes yields the CSTs the walk reads; the C FFI boundary named in the receipt is the entire trusted seam. Known defect held: the grammar cannot parse `<in E>` variance annotations (two ERROR nodes at Filter/FilterGroup, outside the walk), and the twin refuses to emit if any ERROR/MISSING node intersects a byte range it consumed. Output enters gated work solely through the cross-instrument gate (`mise run check:extract-twin`): inventories byte-identical except `extractor.instrument`/`instrumentVersion`, which each instrument fills with its own declared identity — a twin copying them would impersonate the other instrument. Contributes shape facts only; no semantic claims. Version drift of the pin or the C seam is a re-admission event. |

| **RETIRED 2026-08-29** — winkComposer (`winkjs/composer` @ tag `0.5.1` = `a371fd8b`, MIT; operator pin 2026-08-28 — install from the pinned git tag, never the stale npm `wink-composer@0.0.1`). Retired because the row's own gate discharged the row: it required that "every tally must be reproducible by a batch (non-streaming) replay of the same corpus slice", and the landed census instrument's `census:tally` ([experiments/parser-census](../../experiments/parser-census/)) IS that batch replay — a fold over rows on disk with plain integer counters. There is nothing left to stream, and the composer's stated job (moving messages between instrument nodes) is served by two in-process legs and a `for` loop. The row is kept, never deleted: it records what was admitted and why it stopped being needed. Never used in gated work. | Experiment harness for the parser-construct census (`.staging/parser-experiments/`, promoting to `experiments/parser-census/`): streams corpus declaration samples through instrument nodes (tree-sitter twin, TS compiler API, recognition-manifest recognizer) and accumulates per-construct and per-refusal tallies over the strata in the reusable project label set | Plumbing only: it moves messages and sums counters, and contributes NO statistical claim — validity lives in the committed sampling design and deterministic counting rules, and every tally must be reproducible by a batch (non-streaming) replay of the same corpus slice, which is the gate. Sampled evidence at G4 ceiling; nothing it emits enters a formal claim. Version drift is a re-admission event. |

| **RETIRED 2026-08-29** — wink-statistics (`npm:wink-statistics@2.1.1` exactly, MIT, winkjs family; operator pin 2026-08-28). Retired on the same ground as the winkComposer row above: its gate was that "every number it emits must be reproducible by direct batch recomputation from the same committed counts", and the landed census instrument's `census:tally` ([experiments/parser-census](../../experiments/parser-census/)) computes those counts directly, with no estimator, no interval, and no sampling correction — so there is nothing for a statistics package to estimate. The row is kept, never deleted. Never used in gated work. | Streaming and batch summary statistics for the parser-construct census (`.staging/parser-experiments/`, promoting with the lane): per-stratum tallies, dispersion, and interval arithmetic over the counts the composer harness accumulates | Computation aid only: it evaluates arithmetic, and contributes NO statistical claim — estimator choice, sample design, and every reported interval live in the committed experiment design, and every number it emits must be reproducible by direct batch recomputation from the same committed counts, which is the gate. Sampled evidence at G4 ceiling; nothing it emits enters a formal claim. Version drift is a re-admission event. |

| oxc extractor chassis (`oxlint@1.80.0` + `@oxlint/plugins@1.80.0` npm exact; `effect-oxlint@0.3.4` npm = `mpsuesser/effect-oxlint` @ `d8c892f4`, MIT, with `effect@4.0.0-rc.112`; operator order 2026-08-28 "vendor oxlint-effect as the tool") | Production extractor chassis for the store-language recognition lane: v0 spine rules and refusal classifiers authored as oxlint custom rules ([experiments/lift-harness/src/plugin.mjs](../../experiments/lift-harness/src/plugin.mjs), over the engine in [src/oxc-engine.mjs](../../experiments/lift-harness/src/oxc-engine.mjs)), diagnostics carrying canonical-JSON lift/refusal verdicts as hoover-side evidence | Parse facts only, and only through the multi-parser agreement gate: verdicts must be byte-identical to the admitted `typescript@5.9.2` leg on the 265 by-construction fixtures (gate green 2026-08-28: 265/265 including detail strings, 9/9 lifts both legs; wild spot-parity 209/209 files). No semantic or statistical claim; nothing it emits mints an identity, fixture, or manifest rule — the word gate stands. Sampled evidence at G4 ceiling. Version drift of any pin is a re-admission event. |


| fast-check (`npm:fast-check@4.9.0` exactly, MIT; arrives as a pinned transitive of `effect@4.0.0-rc.112` and is pinned directly by the lift harness; admitted 2026-08-28) | Property-test generator for the store-language recognition suite ([experiments/lift-harness/test](../../experiments/lift-harness/test/)): in-grammar v0 programs generated from the manifest as data, and adversarial mutations of them along the declared form axes | Input generation only, and it never decides anything. The adversarial tier asserts ENGINE AGREEMENT, never a particular verdict, so a generator that produced nonsense weakens coverage but cannot manufacture a pass; runs are seeded so any counterexample is reproducible from the record. A shrunk counterexample is evidence for the divergence ledger, never a ruling — rulings are the operator's. Sampled evidence at G4 ceiling. Version drift is a re-admission event. |

| oxc-parser (`npm:oxc-parser@0.147.0` exactly, MIT, oxc-project; admitted 2026-08-28) | In-process parser surface for the oxc recognition leg in the lift-harness suite: `parseSync` produces the ESTree program that `src/oxc-engine.mjs` — the same engine the oxlint chassis runs — recognizes | Parse facts only, and only as the SUITE's invocation surface: the agreement gate still runs the oxc leg through the admitted oxlint chassis, so nothing this package emits enters the gate's evidence. It is a distinct build from the parser inside `oxlint@1.80.0` and the two do not agree on everything — `preserveParens` differs, which is recorded and tested ([T8](../../experiments/lift-harness/test/T8-estree.test.ts)) rather than assumed away. Non-standard node types it emits are audited against the pinned ESTree specification and must be declared before the engine relies on them. Sampled evidence at G4 ceiling. Version drift is a re-admission event. |

| oxc-parser, Stage 1 role (same pin: `npm:oxc-parser@0.147.0` exactly, MIT, oxc-project; role added 2026-08-29 — a ROLE EXTENSION of the row above, not a second admission) | THIRD Stage 1 extractor instrument: `parseSync` over the pinned Effect sources, recognized through the declarative pattern table in [`src/oxc-patterns.ts`](../../experiments/entity-store-extract/src/oxc-patterns.ts), producing the same `inventory.json` body as the other two legs plus `oxc-surface.json`, the census of the full six-file surface | Parse facts only, and it enters gated work solely through `mise run check:extract-oxc`, whose six steps are the trust statement: all six pinned files verified by git blob and parsed with ZERO errors; the five census enumerations agreeing 21/21/21/22/22; and the inventory byte-identical to the committed one and to a live `typescript@5.9.2` extraction, normalizing only `extractor.instrument`/`instrumentVersion` — a leg copying those two would impersonate another instrument. It shares DATA with the other legs (`src/contract.ts`) and no walking code, which is the only reason the agreement carries evidence. What it adds over the tree-sitter twin is READABILITY, not authority: `Schema.ts` and `SchemaTransformation.ts` are unparseable to that grammar at every reachable pin (D1 variance annotations, D1b generic call-signature overloads) and parse clean here — an instrument fact about each instrument, and no argument about the D1 ruling. Contributes shape facts only; no semantic claims. Sampled evidence at G4 ceiling. Version drift is a re-admission event. |

| ESTree specification (`github.com/estree/estree` @ `875bf704`, tree `a61bd75d`; receipt in [.reference/provenance/receipts/estree-spec.json](../../.reference/provenance/receipts/estree-spec.json); admitted 2026-08-28) | Normative reference for the AST shape the oxc recognition leg is measured against; the interface set is extracted mechanically into `experiments/lift-harness/test/estree-standard.json` and drives the deviation audit | Specification text only. It fixes what STANDARD ESTree is and makes no claim about what any parser emits, so every deviation the audit finds is an observation about the parser, never a defect in the spec. It grants no correctness to a recognizer that conforms to it. |


| tsgo / TypeScript 7 (`npm:@typescript/native-preview@7.0.0-dev.20260707.2` exactly, Apache-2.0, Microsoft; operator ruling 2026-08-28 "the official ruling is now TS7". **Role extension recorded 2026-08-29** as the owed re-admission: `library/effects` typechecks under the stable TS7 line — `npm:typescript@7.0.2` exactly, patched by `@effect/tsgo@0.38.0` (`effect-tsgo patch --typescript --oxlint`), which grafts the Effect language service and the `oxlint-tsgolint@7.0.2001` type-aware rules into the same `tsc --noEmit` invocation the `check:effects:ts` gate runs) | Typechecker for the lift harness (`mise run check` → `tsgo --noEmit`) and, in its stable-line role, for the effects package's typecheck gate — superseding the `tsc` 5.x line for typechecking | Typecheck only, and it contributes nothing to any recognition claim: a green typecheck constrains the harness's own source, never a verdict. It does NOT replace the admitted `typescript@5.9.2` compiler-API pin — TS7 exposes no standalone source-text-to-AST parse (its `createSourceFile` is a factory over already-parsed statements; real parses go through the project/program API against files on disk), so the ck recognition leg still parses with the 5.9.2 classic API and that pin stands until the leg is rearchitected. The `@effect/tsgo` patch adds Effect-idiom diagnostics to the gate; those diagnostics are hygiene evidence only and never enter a claim. Version drift of either line (dev-channel or stable) is a re-admission event. |

| litestream (Go, `benbjohnson/litestream`; host-installed binary, never vendored — `0.5.12` on the PC per the run notes in `library/effects/scripts/litestream-check.ts` (its Windows file-URL gotcha was observed at that version), `0.5.16` present on the Mac (`litestream version`, 2026-08-29); version recorded per run; the standing adoption precedent of the Rust/Go hosting-plane ruling, docs/SPECS.md decision 22 addendum; row owed by Wave 3 of decision 23 and by BACKEND-ROBUSTNESS ruling ask R3 — written 2026-08-29) | SQLite replication for the store's byte plane: replicates the one database file that carries both `cas_objects` and `cas_roots`, so bytes and the names that name them replicate together or not at all | Moves bytes, judges nothing; replicate → restore verified by `library/effects/scripts/litestream-check.ts` — after a restore, every Lean-computed address is re-loaded through the full read law (digest recomputed, canonical re-decode, kind checked) and the roots registry is compared exactly, so a page-level corruption anywhere lands as a typed refusal, never as served bytes. The address is the certificate: a replica cannot lie about content, only lose the tail — and replica lag is UNBOUNDED AND UNMEASURED as of this row (the named gap of BACKEND-ROBUSTNESS §"litestream replica stale"; the Wave-3 lag metric is owed). No estate claim attaches to litestream's own durability statements. Version drift is a re-verification event: re-run the check on the new binary before trusting a restore from it. |

| @types/bun / bun-types (`npm:@types/bun@1.4.0` exactly, matching the mise-pinned bun 1.4.0; also pinned as `bun-types@1.4.0` in the experiment harnesses; row written 2026-08-29 closing the register audit) | Ambient TypeScript declarations for the Bun runtime surface, consumed by every typecheck gate that compiles Bun-hosted code | Type declarations only: contributes no runtime bytes, no parse facts, and no claim — a wrong declaration can at worst make a typecheck gate pass or fail wrongly, and every behavior those gates guard is separately executed by the test suites. Pinned to the bun runtime pin; the two move together or the mismatch is a defect. |

| oxlint, effects-lint role (same `npm:oxlint@1.80.0` pin as the extractor-chassis row — a ROLE EXTENSION, not a second admission — with `oxlint-plugin-effect@0.11.0`, `effect-oxlint@0.3.4` (the house-rule authoring API, aligned 2026-08-29 to the chassis row's admitted pin from a drifted 0.3.3), and `oxlint-tsgolint@7.0.2001`; role added 2026-08-29) | The `check:effects:ts` lint gate: native categories at error, the Effect-idiom plugin, and the foldlab house rules (`library/effects/lint/foldlab-rules.ts`) — the estate's laws as lint | Hygiene evidence only: a lint verdict never enters a claim, mints nothing, and gates only the working tree's conformance to the ratified conventions. Named per-file exceptions live in `library/effects/oxlint.config.ts` with the ruling each cites. Version drift of any of the four pins is a re-admission event. |

| Effect runtime family (`npm:effect@4.0.0-rc.112` exactly, plus `@effect/platform-bun`, `@effect/sql-sqlite-bun`, and `@effect/vitest` at the same rc; MIT, Effect-TS; estate-wide move ruled as Wave 1, docs/SPECS.md decision 23; provenance: the `effect-runtime` row of `.reference/provenance/sources.lock.json` names upstream commit `2600f62f` = tag `effect@4.0.0-rc.112`, and `library/effects/package.json`'s `foldlab.effectProvenance` names that row back) | The runtime substrate the effects package ships on and the harnesses execute against — the L0 execution plane whose behavior the Lean model describes | The substrate under test, never a trust source: every correspondence claim about it is carried by the gates and the Lean model, and its own documentation or types prove nothing here. The npm version and the provenance lock row must name each other (AGENTS.md dependency law); moving the version is an estate-wide ruling event, never a routine bump, and the Stage-1 extraction pin (`effect` lock row, rc.111) deliberately does NOT move with it. |

| codex CLI (`codex-cli@0.146.0`, OpenAI; models `gpt-5.6-sol`, `gpt-5.6-luna`) | Reference-material tagger/ingestion assistant: produced `.claude/skills/implement/book-tags.json` from the pinned book (the `program-proofs` row, `.reference/provenance/sources.lock.json`) on 2026-08-30, 10-agent wave | Output is untrusted reference projection; every claim cites a book section and is verifiable against the pinned source; nothing it produces enters gated/formal work without operator grilling. |

| codex CLI, scout-annotator role (same `codex-cli@0.146.0` pin as the row above — a ROLE EXTENSION, not a second admission; `gpt-5.6-luna` primary, `gpt-5.6-sol` capped at one planning call per run; role added 2026-08-30) | Bank annotator and candidate proposer for the model-scout lane (`.staging/model-guided-development/`): annotates obligation-family entries and proposes atomic candidate invariants under the lane's harness law ([ANNOTATE.md](../../.staging/model-guided-development/ANNOTATE.md) — read-only sandbox, schema-constrained output, every call receipted with prompt and output digests) | Empty trust contribution, always: an annotation is a search hint. Output lands only in `## Annotations` blocks and candidate lists inside the lane; a curating session's move into curated fields is the acceptance act and carries the receipt digest; nothing it writes enters a packet, gate, doc, or formal artifact without an independent checker or the operator's grill, and lane statuses are working language, never gate stamps. Version drift of CLI or model is a re-admission event. |

| liteparse (mise shim; `npm:@llamaindex/liteparse`, PDF/document parser) | Text projection of pinned PDFs: produced the markdown projection of `.reference/papers/program_proofs.pdf` (the `program-proofs` row, `.reference/provenance/sources.lock.json`) | Mechanical extraction with OCR fallback; prose is reliable, code blocks may carry OCR artifacts; projections are working copies, never the source of truth — the pinned PDF is. |

Pending admission (not yet used in gated work): wink-naive-bayes-text-classifier
(`npm:wink-naive-bayes-text-classifier@2.2.1` exactly, MIT, winkjs family;
pinned in the lift harness). A classifier aid inside the harness's sieve
(`experiments/lift-harness/src/rung/Sieve.ts`) — exploratory ranking only,
outside the chain (`check:lift-roundtrip` is not in `mise run check`), and
nothing it emits enters a gate, a fixture, or a count. A ruling is owed
before any gated artifact depends on its output; noted here 2026-08-29 so
the register's dependency audit stays closed.

Pending admission (not yet used in gated work): oxc-transform
(`npm:oxc-transform@0.147.0`, pinned and installed in the lift harness).
Explored 2026-08-28 as a candidate ERASURE ORACLE for the recognizer's
type-blindness: `transformSync` under `onlyRemoveTypeImports: true`
(the `verbatimModuleSyntax` semantics) reports per-specifier which imports
survive to runtime, syntactically and without a type-checker. No gated
work runs it — the finding is recorded on the divergence ledger as the
open contract gap `TG1`, and a ruling is owed before it becomes machinery.

Promoted 2026-08-30: liteparse (`npm:@llamaindex/liteparse`) is superseded
by the formal register row above — it produced a projection now cited from
gated documentation (the `.reference/papers/program_proofs.pdf` markdown projection), so it
carries a trust statement rather than a pending-admission note. It remains,
as before, the extractor behind the
[paper corpus](../../.reference/catalog/PAPERS.md).
