# Go code quality audit — condensed record

Provenance: Opus auditor, 2026-08-13, report-only over go/ and
proto/go (18,889 lines). Tools run clean: gofmt, vet, go test -race
(both suites PASS today), govulncheck (0 called vulns), staticcheck
(ONE issue total). Full findings F1–F18 with file:line lived in the
audit report; this record carries the verdicts, the ratified
dispositions, and the execution pointer (scratch task 20).

## Verdict

Two codebases: the SUBSTRATE (canonical, stream, journal, effector,
protod) is near-spotless — correct mutex placement for subtle reasons,
zero-copy discipline holding everywhere traced, explicit integer
boundaries, zero TODOs. The HARNESSES (climb, crashstorm, transfleet,
realrun) are soft in the worst place: they produce the evidence the
frozen verifiers judge. The asymmetry — verifiers held to a far higher
standard than the machinery feeding them — is the finding behind the
findings.

## Verdict-integrity findings

- F1 climb spend-cap check-then-act race: four workers can pass at
  cap-1 and each buy — real-money overspend handing the verifier a
  bundle it must refuse: a MANUFACTURED REFUSAL. Loaded, never fired.
- F2 crashstorm respawns workers that exited because they CAUGHT a
  violation, then deletes their logs — a finding converted to timeout
  with evidence destroyed. RATIFIED: unexplained exits abort and
  preserve; respawn only controller-caused kills.
- F3 realrun's early-death guard is dead code (ProcessState before
  Wait).
- F8 frozen verifier compares an error by message string — the one
  string-compare in the repo, in the verifier.
- Tooling: -race runs NOWHERE in gates (23s, both suites pass today);
  the sabotage build tag is never compiled by any gate; zero fuzz
  corpus entries ever committed.

## Ratified dispositions (operator, 2026-08-13)

F2 abort+preserve; F7 full shutdown handshake (drain/cancel/bounded
wait/Once, 30s default request timeout as Options field, error
handler); F9 bad-stream/bad-bucket named refusal kinds; F11 reads
capped 1000 with partial+continuation (aligns with get-batch
migration); F12 normalizeUnions hoisted explicit; F14 floors as
copy-returning funcs, scheme as Daemon field (laws are not mutable
globals).

## Graduation note

No Go-side JCS duplication (canonical is already the single
implementation — the expected landmine is absent). Real landmines:
utf16Less duplicated byte-identically (export canonical.UTF16Less
BEFORE the move); wireEntry defined four times (promote to
journal.WireEntry); protod hardcodes "J_"+name (export
journal.StreamName); the sabotage tag's gate responsibility moves
with graduation. go.sums are identical; the replace-drop is
mechanical; go/daemon/AGENTS.md's status table is the graduation
instrument — keep it updated DURING the move.

## Keep-as-is (explicitly cleared)

Laws-as-comments style (doing real work; do not normalize); the
append/frozenPath aliasing discipline in walk.go; effector clock
usage (wall time affects only takeover timing — exactly what the
theorem requires); no genuine data race found in the fleets by
two sweeps.

## Execution

scratch/codex/20-harness-integrity.md carries all of it: A
verdict-integrity fixes first, B refusal-contract + lifecycle, C
latent/consistency + harness tests (crashstorm and transfleet
currently have ZERO test files), D tooling into gates (-race, tagged
vet/build, pinned staticcheck, weekly govulncheck, fuzz-corpus
policy, exported-surface golden wall), E godoc law pass.
