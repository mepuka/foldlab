# DEV-671 adversarial review — three-lens Rev report

Target: `agent/codex/DEV-671` @ `a25677730` ("totalize move refusal
semantics"). Three independent Opus 5 reviewers, 2026-08-15:
statement fidelity (read-only), mechanical gates (isolated worktree,
perturbation-tested), adversarial mathematics (kernel-checked
counterexamples against the branch's own definitions). Verdicts:
**FAITHFUL-WITH-NOTES / GATES-HOLD-WITH-GAPS / SOUND-BUT-OVERSTATED.**
Nothing false was found anywhere. Merge disposition and ratification
questions at the end.

## Independently confirmed clean

- Gate green cold (5.2s, twice); all 24 rostered reports exactly
  `{propext, Classical.choice, Quot.sound}`; no `sorryAx`, no user
  axiom; roster arithmetic 17+7=24 with all seven new names present.
  (Confirmed twice over: the math reviewer staged its own build.)
- **Zero deleted lines** in either Lean file — purely additive; no
  pre-existing statement, definition, or hypothesis could change
  meaning. `TerminalCarries`, `Recorded`, `WF`, `step`, `repair`,
  `Runs` byte-identical to main.
- `stepK`/`repairK` totality exactly per spec (refused = same state +
  `false`, definitional); agreement lemmas verbatim to spec, and the
  pair is a complete characterization — all converses derivable
  (kernel-checked). D79's keep-the-boolean rationale confirmed
  load-bearing: without the flag, an admitted self-loop is
  indistinguishable from a refusal.
- The vacuity control is a genuine refutation: `Runs` is uninhabited
  on the three-fill bag across all six permutations (falsification
  attempt failed; `runRepair … = none` confirmed executably); the
  total runner's `[true, true, false]` observation list confirmed by
  `#eval` byte-for-byte.
- Executable spot-checks: refused move under `stepK` returns the
  unchanged state and `false`; admitted matches `step`.
- Codex's board closing report: every factual claim checks out,
  including the delicate one — it states the disjunction honestly.
- No false MOVES-5 disposition, no code-correspondence claim, daemon
  caveat intact.

## Findings (convergent, most severe first)

**R1 — `no_lossK` names more than it says (all three reviewers).**
Its conclusion is `refused-on-record ∨ terminally-retained` —
strictly weaker per-conclusion than `no_loss`. This is **forced, not
a cheat**: the strong form is false under ratified D1–D3 repair (a
fill refused at a disputed hole leaves no trace in meaning or
evidence — `¬TerminalCarries` and `¬Recorded` for value 30,
kernel-checked). But the statement shape is satisfied by a runner
that refuses everything (3-line proof), the real content lives in
un-rostered `no_lossK_admitted` (itself a lifting of old `no_loss`
to the admitted subsequence), and the name invites over-reading by
exactly one disjunct. Honest reading: **no *silent* loss.**

**R2 — `runRepairK` is not confluent, and the branch does not say
so (math reviewer; kernel-checked).** `[10a,20b,30x]` terminates at
dispute `{(10,a),(20,b)}`; the permutation `[10a,30x,20b]` at
`{(10,a),(30,x)}`. Which value survives ≥3-way contention depends on
arrival order; both outcomes satisfy `no_lossK`. The legacy
confluence/fence theorems do NOT lift to the total runner. Worse,
the ledger edit *removed* the old explicit refused-move-divergence
disclosure and replaced it with a closure claim.

**R3 — `runK` is inert (all three).** Defined, documented with an
alignment property, proved by nothing, rostered nowhere, consumed by
nothing. Spec item 3 satisfied definitionally and mathematically
empty. Related: D79's input-alignment invariant
(`obs.map Prod.fst = intents`) is prose-only — no theorem states it,
and the vector wall inherits it unproved.

**R4 — roster optics inverted (math + fidelity).** Four of the seven
new roster slots are true-by-construction unfolding lemmas;
`no_lossK_admitted` (the substantive theorem) is un-rostered
(footprint covered transitively; visibility inverted). "Seventeen
gated results" → "twenty-four gated axiom reports" wording change was
made honestly everywhere — credited.

**R5 — record hygiene (fidelity).** The audit's MOVES-1 header says
"closed" while its own body says the vector-wall half is pending
(VERIFICATION.md gets it right: "closed at the model layer"). The
dated audit record's original disposition sentence was rewritten in
past tense rather than appended-to. Board status still `todo` under
the completion comment. VERIFICATION.md's bounds prose implies all
legacy theorems except `no_loss` are total (the table row states it
correctly).

**R6 — pre-existing gate defects exposed (mechanical; NOT DEV-671's
fault).** (a) `exact sorry` and `private axiom` evade the source
greps; caught by the axiom check only if a rostered theorem reaches
them. (b) **Orphan theorems are wholly ungated** — demonstrated by
proving a false lemma green. (c) The README claim "every headline
theorem and control" is rostered is false: seven README-table
theorems are absent, and `dispute_merge_semilattice` was
`exact sorry`'d with the gate staying green. (d) Statement weakening
is invisible to every gate — `(h : False)` added to `no_lossK` and a
silent revert to admitted-only both passed green (confirmed by
execution). Definition drift, by contrast, IS caught (laundering
refusals as admitted broke three proofs).

## Recommended disposition

**Accept the mathematics; repair the framing before merge** (same
branch, small follow-up commit):

1. Rename `no_lossK` → `no_silent_loss` (or operator ratifies the
   name); roster `no_lossK_admitted`.
2. Prove the **sublist factorization** (total run's terminal = a
   legitimate partial run over the admitted subsequence) — turns the
   headline into a corollary with visible provenance; the vector
   wall needs it anyway.
3. Commit the non-confluence disclosure + the reviewer's two
   kernel-checked probes (`p2_not_confluent`,
   `p3_twenty_lost_under_B`) as a new negative control; restore the
   refused-move-divergence sentence in VERIFICATION.md.
4. Fix the MOVES-1 header ("closed at the model layer; wall half
   pending"); append-not-rewrite the audit disposition.
5. Add `¬TerminalCarries … h 30` to the vacuity control and have it
   exercise `no_lossK` itself; prove the D79 alignment lemma.
6. `runK`: prove its agreement + alignment or delete it (the lawful
   surface rule: no function without its law).

Separately (pre-existing, new issue): gate hardening — widen the
grep patterns, roster or de-advertise the seven README theorems,
note the statement-blindness limitation in verify/AGENTS.md with the
Rev-review mitigation.

## Ratification questions for the operator

- **Q1:** Accept "no loss" as naming *no-silent-loss* under the
  total runner, or require the rename (recommended: rename)?
- **Q2:** Non-confluence under ≥3-way contention is a real property
  of ratified D1–D3 repair (two-party repair only). Disclose now
  (recommended) and defer any semantics change (e.g., third fills
  merging into open disputes) to its own D-numbered decision?
- **Q3:** Repair-then-merge (recommended) vs merge-then-repair?

Note: the primary checkout is currently on `agent/codex/DEV-671`
(seat rule says it stays on `main`); the coordinator restores it at
merge time.
