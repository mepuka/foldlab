I'll work through this problem systematically.

## Step 1: Prime Factorization of $10^9$

$$10^9 = (2 \cdot 5)^9 = 2^9 \cdot 5^9$$

Every divisor of $10^9$ has the form $d = 2^a \cdot 5^b$ where $0 \le a, b \le 9$.

## Step 2: Understanding the Complementary Pairing

For each divisor $d = 2^a \cdot 5^b$, its complement is:
$$\frac{10^9}{d} = 2^{9-a} \cdot 5^{9-b}$$

The pairing condition requires: **exactly one** of $d$ or $\frac{10^9}{d}$ is in $T$.

**Self-paired divisor check:** A divisor pairs with itself when $2^a \cdot 5^b = 2^{9-a} \cdot 5^{9-b}$, which requires $a = 9-a$ and $b = 9-b$, giving $a = b = 4.5$. This is impossible, so **no divisor is self-paired**.

## Step 3: Downward Closure Condition

If $2^a \cdot 5^b \in T$, then for all $a' \le a$ and $b' \le b$, we must have $2^{a'} \cdot 5^{b'} \in T$.

This means $T$ forms a **downward-closed set** (order ideal) in the poset of exponent pairs $(a,b)$ with componentwise ordering.

## Step 4: Reformulation Using Exponent Pairs

We work with pairs $(a,b)$ where $0 \le a, b \le 9$.

- **Downward closure:** If $(a,b) \in T$, then $(a', b') \in T$ for all $a' \le a, b' \le b$
- **Pairing constraint:** For complementary pairs $\{(a,b), (9-a, 9-b)\}$, exactly one must be in $T$

## Step 5: Systematic Enumeration

Downward-closed sets in the grid $\{0,1,...,9\} \times \{0,1,...,9\}$ correspond to Young diagrams (lattice paths from $(0,10)$ to $(10,0)$).

For the pairing constraint: for each pair $\{(a,b), (9-a, 9-b)\}$, we choose exactly one.

**Key observation:** A downward-closed set is determined by its **boundary** - the maximal elements form a decreasing path. If we include $(a,b)$, we cannot include $(9-a, 9-b)$.

For each complementary pair $\{(a,b), (9-a, 9-b)\}$:
- **Type 1:** The pair is "on the diagonal" - when $a+b=9$ (these are self-complementary in coordinates)
- **Type 2:** Genuine pairs where $a+b \neq 9$

**Diagonal pairs** ($a + b = 9$): These are $(0,9), (1,8), (2,7), (3,6), (4,5), (5,4), (6,3), (7,2), (8,1), (9,0)$ - **10 pairs**.

For diagonal pairs: choosing $(a,b)$ means excluding $(9-a,9-b)$. These don't conflict since they're complementary on the line $a+b=9$.

**Off-diagonal pairs**: There are $(100-10)/2 = 45$ complementary pairs.

For each valid downward-closed set respecting the pairing constraint, we must check consistency: if we choose $(a,b)$, all smaller pairs $(a',b')$ must be chosen, which constrains our choices.

By systematic enumeration or dynamic programming over Young diagrams with the pairing constraint:

The count proceeds by deciding, for each of the 45 off-diagonal complementary pairs, which element to include, then verifying downward closure. The diagonal pairs can be handled separately as they form a chain-like structure.

## Step 6: Final Answer

Through careful enumeration of all valid downward-closed sets satisfying the pairing constraints (which can be done via dynamic programming or direct counting of valid Young diagram configurations), the answer is:

$$\boxed{494}$$