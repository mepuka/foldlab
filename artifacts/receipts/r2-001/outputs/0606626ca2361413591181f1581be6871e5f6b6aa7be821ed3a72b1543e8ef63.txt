I'll work through this plan systematically to verify the solution.

## Step 1: Identify the tangency requirement

For the equation $|x^2 + ax + b| = c$ to have exactly 3 integer solutions, we need one horizontal line to be tangent to the parabola $y = x^2 + ax + b$ (1 solution) and the other to intersect it at exactly 2 points.

This means:
- Either $x^2 + ax + b = c$ has 1 solution and $x^2 + ax + b = -c$ has 2 solutions
- Or $x^2 + ax + b = c$ has 2 solutions and $x^2 + ax + b = -c$ has 1 solution

## Step 2: Set up vertex conditions

The vertex of $f(x) = x^2 + ax + b$ is at $x = -\frac{a}{2}$.

For tangency at an integer point, the vertex must be at an integer $x$-coordinate.
- Require $a = 2m$ for integer $m$
- Vertex is at $x = -m$

## Step 3: Express the vertex value

At $x = -m$:
$$f(-m) = (-m)^2 + a(-m) + b = m^2 - 2m^2 + b = b - m^2$$

Let $v = b - m^2$ be the vertex value.

For exactly 3 solutions total, one of the lines $y = c$ or $y = -c$ must be tangent (equal to $v$), and the other must intersect at 2 points.

## Step 4: Analyze the two cases

**Case 1:** Tangent at $c = v = b - m^2$
- Then $-c$ must satisfy: $x^2 + 2mx + b = -c$ has exactly 2 integer solutions
- This means: $x^2 + 2mx + b + c = 0$
- Since $c = b - m^2$: $x^2 + 2mx + m^2 + 2c = 0$
- Or: $(x + m)^2 = -2c$

This is impossible for real solutions if $c > 0$.

**Case 2:** Tangent at $-c = v = b - m^2$
- Then $c$ must satisfy: $x^2 + 2mx + b = c$ has exactly 2 integer solutions
- This means: $x^2 + 2mx + (b - c) = 0$
- Since $-c = b - m^2$, we have $b = m^2 - c$
- So: $x^2 + 2mx + (m^2 - 2c) = 0$
- Or: $(x + m)^2 = 2c$

For 2 integer solutions, we need $2c$ to be a perfect square.

## Step 5: Determine constraints on c

Let $2c = k^2$ for some positive integer $k$.

Then $(x + m)^2 = k^2$ gives $x + m = \pm k$, so:
- $x = -m + k$ and $x = -m - k$ (two integer solutions ✓)

For the tangent case: $(x + m)^2 = 0$ gives $x = -m$ (one integer solution ✓)

**Key constraint:** $2c$ must be a perfect square.

So $c = \frac{k^2}{2}$ where $k$ is a positive integer.

For $c$ to be an integer: $k$ must be even. Let $k = 2j$ for positive integer $j$.

Then $c = \frac{4j^2}{2} = 2j^2$.

## Step 6: Count valid values

We need $1 \le c \le 2025$ where $c = 2j^2$ for positive integers $j$.

$$2j^2 \le 2025$$
$$j^2 \le 1012.5$$
$$j \le 31.8...$$

So $j \in \{1, 2, 3, \ldots, 31\}$.

This gives $c \in \{2, 8, 18, 32, 50, 72, 98, \ldots, 2 \cdot 31^2\}$.

The number of valid values is **31**.

## Final Answer

The number of possible values of $c$ is **31**.