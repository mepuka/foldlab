# .reference

Read-only study material.

- `core-concepts.md` — the compilation of the 2026-08-11/12 playground-mech
  sessions: the two folds, the effector theorems, merge/fork/compaction/
  replay laws, entity boundaries, Effect/Schema semantics, the LLM framing.
- `playground-mech/` — mirrored artifacts from the playground-mech clone
  (branch `mech/model-gate`): research reports, the climb log, TLA+ specs
  and configs (incl. the committed two-key counterexample), the Go effector
  model + conformance driver, and the mech TS checker with its law suites.
  These are copies for reference; the living versions stay in that repo.
