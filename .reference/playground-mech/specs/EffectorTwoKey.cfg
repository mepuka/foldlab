\* The withdrawn two-key protocol at the same gate shape.  TLC MUST report
\* a violation here (fencing safety), with a minimal 4-step counterexample:
\* claim, begin, steal, finish — the shape that refuted the protocol.
SPECIFICATION Spec
CONSTANTS
  NumOwners = 3
  TwoKey = TRUE
  AdversarialSteal = TRUE
  SelfInterleave = FALSE
  AllowCrash = TRUE
  FenceCap = 3
INVARIANTS TypeOK TerminalFenceIsMaximal
PROPERTIES FencingSafety UniqueTerminalOutcome GenerationsAreMonotone
