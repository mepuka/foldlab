\* Negative control for the induction harness: the WITHDRAWN two-key
\* protocol under the same obligations.  SafetySteps from IndInit MUST fail
\* here (the finish never consults the revision, so a stale commit lands
\* below maxFence) — the refuted protocol refuted again at the induction
\* level.  See EffectorInd.tla.
CONSTANTS
  NumOwners = 3
  TwoKey = TRUE
  AdversarialSteal = TRUE
  SelfInterleave = FALSE
  AllowCrash = TRUE
  FenceCap = 0
INIT Init
NEXT Next
