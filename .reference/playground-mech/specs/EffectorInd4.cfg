\* Induction at FOUR owners (the Go model's MaxOwners width): the invariant
\* is owner-count-generic, and this configuration checks that claim at the
\* widest width the canonical encoding carries.  FenceCap = 0: unbounded.
CONSTANTS
  NumOwners = 4
  TwoKey = FALSE
  AdversarialSteal = TRUE
  SelfInterleave = FALSE
  AllowCrash = TRUE
  FenceCap = 0
INIT Init
NEXT Next
