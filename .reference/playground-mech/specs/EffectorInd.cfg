\* Apalache induction configuration: single-key gate shape with FenceCap = 0
\* — generations UNBOUNDED.  Init/inv are chosen per obligation on the CLI
\* (see packages/mech/scripts/modelcheck.ts and the model-gate report).
CONSTANTS
  NumOwners = 3
  TwoKey = FALSE
  AdversarialSteal = TRUE
  SelfInterleave = FALSE
  AllowCrash = TRUE
  FenceCap = 0
INIT Init
NEXT Next
