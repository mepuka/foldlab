# MECH climb log — the model gate (effector lane)

One line per attempt: `attempt N: <what changed> -> <result>`.
Never delete prior lines — the climb history is data.

attempt 1: read SPEC §6 (as amended), P4's ratification note, the proof-tooling briefing and the audit's bounded transition check; ran the baseline gates -> Go canonical/journal/effector/cmd green; `gofmt -l .` lists ALL eight pre-existing Go files because this clone has core.autocrlf=true (index LF, worktree CRLF), so the gofmt-emptiness gate is unsatisfiable here for reasons that predate this lane
attempt 2: wrote go/effector/model — abstract single-key and two-key protocols, commit split into begin/finish, canonical fixed-width state encoding, deterministic BFS with parent-link trace reconstruction -> single-key clean at depth 12 (3 owners, adversarial steal, crash-stop): 172,214 states / 708,876 transitions / 0.20s
attempt 3: pointed the same checker at the withdrawn two-key protocol -> fencing violation REDISCOVERED at minimal depth 4, trace claim(A), begin(A), claim(B), finish(A); the single self-interleaving owner variant reproduces the audit's recorded begin/steal/finish shape verbatim
attempt 4: added the generation-capped closure mode and the lease-gated (non-adversarial) variants -> single-key clean to closure at 2/3/4 generations (584 / 2,312 / 6,848 states); two-key fails under lease-gated steals too, so the defect is not an artifact of the adversarial relaxation
attempt 5: added the determinism harness (nine identical in-process runs per protocol) and the encoding-injectivity check -> identical state counts, transition counts, fingerprints and counterexample traces across runs; 172,214 encodings all distinct
attempt 6: added the split-vs-unsplit comparison over every reachable paused commit -> single-key 292,740 compared / 0 differ (the client-side pause is observationally inert), two-key 365,910 compared / 38,766 differ (the pause is decisive — it IS the bug)
attempt 7: added the fairness-bounded progress check, first cut -> 90 false "stuck" states at generation cap 2; they were states the CAP starved, not the protocol
attempt 8: split the progress verdict into interior states and cap-truncated states -> 0 stuck at caps 2/3/4, longest route to a terminal outcome 4 transitions, cap-truncated states counted and reported rather than dropped
attempt 9: built the trace-conformance driver — per-owner effector bindings over an interposed jetstream.KeyValue that stalls the caller between its fence read and its protected write, giving a GENUINE client-side pause without touching the implementation -> cex-adjacent depth 6, 276 schedules, 1,656 steps, 0 divergences
attempt 10: added the exhaustive and crash-bearing sweeps, lease planning by lookahead, and counted lease-overrun retries -> exhaustive d5 1,422 schedules / crash d4 1,197 schedules / 0 retries / 0 divergences, whole package 4.9s
attempt 11: ran the deep sweep (MODEL_GATE_DEEP=1) for the first time -> DIVERGENCE at crash-d5 #5038: a paused commit whose fence read already showed the terminal outcome absorbs idempotently without ever attempting a write, so the crash had no write to stop; harness bug, model and implementation both right
attempt 12: reworked the crash assertion to assert what crash-stop actually claims — no WRITE lands, with substrate refusal required only of a commit that was about to write -> deep sweep 15,378 schedules / 88,743 steps / 0 divergences
attempt 13: added mutation self-validation of the conformance driver (a refused commit predicted to land, a claim at the wrong generation, an unclaimed state predicted committed) -> 828/828 corrupted schedules caught; also closed a hole where a schedule that exhausted its lease budget was recorded as neither pass nor fail
attempt 14: moved worker construction to the test goroutine (t.Fatalf off-goroutine would Goexit the wrong one), registered paused commits before the select so no goroutine can outlive its schedule, and asserted against acting as an already-crashed process -> gate green, -race clean
attempt 15: re-ran the deep sweep -> DIVERGENCE at crash-d5 #5486, a FALSE POSITIVE: the lease-overrun classifier tested liveness against the state BEFORE the action, so a claim creating a short-leased generation (Absent before, live after) had its lookup artifact reported as an implementation divergence
attempt 16: classifier now takes the state whose live-claim assumption the failed assertion rests on (Before for outcomes, After for lookups) and the lease budget was raised to ~10x the work it covers -> deep sweep 15,378 schedules / 88,743 steps / 1 counted lease retry / 0 divergences
attempt 17: wrote docs/research/effector-model-gate.md and ran every gate -> TS typecheck clean + 61/61; Go vet clean; scoped Go tests green; go test -race ./effector/... green; model package 11.8s at gate depth
attempt 18: built packages/mech (System layer: deterministic BFS, transition invariants, minimal traces, FNV-1a-64 fingerprints, path enumeration, Effect surface via checkEffect/MechViolation) -> compiles under the workspace strict flags
attempt 19: ported the effector model to TS with byte-identical canonical encoding and pinned the Go numbers as frozen fixtures -> ALL NINE wall tests green on the first run: 172,214 states / fingerprint 5cb6d2203cb19cce and every other pin exact, both counterexample traces action-identical
attempt 20: built the Scenario layer (workers as generator code, exhaustive deterministic schedule exploration, replayed prefixes) and the A6 demo -> explorer flagged a legal schedule: my DEMO PROPERTY was wrong (fencing safety stated as a state predicate; a commit legal when it linearized is not retroactively illegal when a later claim raises maxFence)
attempt 21: moved the demo property to the write itself (max-generation-at-commit-time) -> two-key refuted with the exact CEX-3 schedule from ~30 lines of readable worker code; single-key clean over all schedules (14 two-worker, 450 three-worker, exhaustive)
attempt 22: wrote the guarded-activity-protocol demo (P4 Part 2 shape: lookup/claim/poll/commit/adopt) -> 426 schedules clean, EF3 foreign-outcome adoption zero-execution; first sabotage attempt NOT caught (a worker that never commits lets the honest engine give up polling, so no disagreement materializes) — harness lesson recorded
attempt 23: rewrote the sabotage to corrupt adoption (journal a local fabrication over the authoritative fact) -> caught with the disagreeing schedule printed; guarded demo 3/3
attempt 24: full gates after fixing a claim-result inference error -> bun run typecheck clean; bun test 83/83 (61 kernel + 22 mech, ~20s); wrote docs/research/mech-library.md
attempt 25: audited the report against primary sources, corrected the stretch from fairness/liveness to bounded EF Done reachability, and replaced BigInt-per-byte FNV with exact 32-bit limbs after the full gate exposed a 5s timeout -> typecheck clean; Bun 83/83 twice; scoped Go plus race green; deep 15,378 schedules / 88,743 steps / 0 divergences
attempt 26: built real-engine conformance — the actual layerJournalGuarded over a gated in-memory §6.1 register, virtual clock through the overridable Clock reference (TestClock exists at the pin but would put time-advance into the schedule alphabet for zero gain in the no-lapse regime; named for the future lease-lapse lane), stateless schedule exploration with greedy corridor extension -> 86 complete schedules / 0 divergences / 207 runs / 639ms on the FIRST run; foreign-outcome variant adopts with zero executions; three runs of one schedule byte-identical
attempt 27: added substrate self-validation (a register granting a live claim again at the same fence) -> caught at the TOCTOU schedule [0,1,0,1,0,1,1] where B's claim lands between its own lookup and A's; executions=2 fires exactly-once; the engine's lookup-before-claim discipline masks the defect on nearly every other schedule, so reaching it REQUIRED the explorer
attempt 28: fixed the Clock interface (effectful members missing) and a commit return-type inference pin; full gates -> typecheck clean; bun test 87/87 (~21s); conformance suite 4/4 in ~1s; report updated (mech-library §4)
attempt 29: wrote the production spec proposal (docs/research/mech-production-spec.md — determinism/coverage/falsifiability/fixtures as production laws, module layout, hardening backlog) and the algebraic layer src/proc.ts (pure/map/flatMap/andThen over Proc, plus the deliberately unlawful flatMapWithTick) with behaviors() observational equivalence in scenario.ts -> compiles clean
attempt 30: ran the ML/WL law suite -> 9/9 first run: ML1-ML6 (functor/monad/Kleisli) hold observationally against a rival worker; ML7 catches the ticked bind (6 schedules lawful vs 10 ticked — an extra linearization point changes the schedule space while every value agrees); WL1 monad-law regrouping preserves the REAL layerJournal's journal byte-for-byte with compositional replay and zero re-executions; WL3 a path-renaming refactor keeps the value and forks the history (SPEC 8.1's caveat demonstrated, evidence toward PO-19)
attempt 31: full gates -> typecheck clean (ELS's only remark on the suite is suggesting the right-identity simplification on the very line that exercises right identity — annotated as deliberate); bun test 96/96
attempt 32: wrote specs/Effector.tla — the SPEC 6.1 transition table as TLA+ (register as a tagged-record ADT, commit split into Begin/Finish, crash-stop, a TwoKey constant selecting the withdrawn protocol stated in the SAME table) -> TLC closure counts match the pinned Go/TS numbers EXACTLY on the first run: 584 / 2,312 / 6,848 distinct states at generation caps 2/3/4 (TLC generated counts = pinned transition counts + 1)
attempt 33: pointed TLC at EffectorTwoKey -> fencing violation REDISCOVERED with the minimal 4-step trace claim(1), begin(1), claim(2) steals, finish(1) lands Done(f=1) under maxFence=2; artifact committed as specs/EffectorTwoKey.cex.txt; two runs identical modulo timestamp/pid/seed header lines
attempt 34: wired `bun run modelcheck` (three single-key pins green + two-key red with the trace shape asserted) plus the CI-shape gate test, and the TLC-dump parity test: parse every dumped state, canonicalize through the TS encoder, exact SET equality with the TS closure at caps 2 and 3 -> 4/4 green in ~5s, 2,913 assertions
attempt 35: made the spec Apalache-ready (Snowcat type aliases and operator annotations, RegisterType without record-set membership, Owners filtered from a literal range around the symbolic-bound known issue) -> typecheck "purrfect"; re-ran TLC pins green
attempt 36: downloaded Apalache 0.61.0 (sha256-verified against the release manifest, gitignored under tools/apalache) and wrote EffectorInd.tla — candidate invariant = TypeOK + absent-means-unstarted + claim-tracks-max + done-tracks-max + handle discipline + the freshness lemma (a fresh snapshot IS the current register) -> ALL FOUR obligations proved on the FIRST candidate: Init=>Inv, Inv/\Next=>Inv (5.7s), Inv=>TerminalFenceIsMaximal, Inv/\Next=>FencingSafety/\UniqueTerminal/\Monotone — SPEC 6.2/6.3 now hold UNBOUNDED (arbitrary fences, arbitrary depth) at 3 owners
attempt 37: negative controls, because a prover that cannot fail proves nothing -> consecution WITHOUT the freshness clause refuted (the clause is the revision CAS, not decoration); the two-key protocol under the same action-safety obligation refuted with the stale commit Done(f=2) under maxFence=3; wired both into `bun run modelcheck:ind` (6 obligations, 4 must-green 2 must-red)
attempt 38: repeated all four proof obligations at NumOwners=4 (the encoding's full width) -> all proved; wired the 10-obligation suite into `bun run modelcheck:ind`; report amended (sections 10-12: TLC counts/depths, the committed two-key trace, the TLA+=TS=Go wall, the Apalache verdict with the failed-control evidence, revised SPEC 10.1 placement — proof for 6.2/6.3 unbounded fences+depth at 3 and 4 owners; owner count and substrate abstraction remain bounds; conformance remains integration-grade)
attempt 39: answered "does safety need one id per node?" with a theorem — IndInvIdentityFree drops the handle-discipline clause (the only identity assumption), keeping just the register algebra + freshness, checked under SelfInterleave=TRUE (one identity, concurrent workers; strict superset of behaviors) -> all four obligations proved; TLC bounded companion (self-interleave, cap 3) clean at 2,750 states and dump-set-equal to the TS model; induction suite now 14 obligations; report 10-12 amended with the substrate-assumption statement (K1/K2 + linearizable reads + no deletion of Done; identity is payload, never premise)
attempt 40: built the stream lane (go/stream + packages/mech/src/stream.ts, byte-identical over a frozen go/cmd/streamfix fixture wall) — canonical event encoding, SHA-256 chain heads, merge facts as committed linearizations, last-write-wins KV fold, compaction, fork segments, gzip transport -> Go 7/7; TS wall reproduces every Go digest byte-for-byte; laws SL1-SL6 green including SL1 "the chain remembers what the fold forgives" (cross-key swap: fold digest equal, merged head different) and SL5 fork-resolution-as-fenced-commitment (two fork heads raced through the proved single-key effector over every 2-worker schedule: at most one canonical head, loser adopts); one API correction (Effect.reduce zero is lazy at beta.107)
