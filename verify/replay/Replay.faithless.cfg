\* Faithless control: no ready guard — a node may commit against absent
\* inputs read as defaults. MUST be refuted on SpecEval (the TLA twin of
\* lean/Replay/Faithless.lean's divergence counterexample).
SPECIFICATION Spec
CONSTANTS
  Guard = "faithless"
  MaxFence = 3
INVARIANT
  SpecEval
