{
  "$comment": "The kernel language projected as MCP tool input schemas, generated from the kernel model's projection AST and the reviewed wire-convention manifest. Shapes follow the ruled wire leans: eight flat tools, no oneOf, compound self-descriptive field names (a field is named for the value it carries), digests as prefixed opaque strings with pattern, integers only and EXACT (an identity coordinate is an arbitrary-precision non-negative integer with no ceiling; a client that parses one as a double loses values the canonical grammar requires; no floats exist in that grammar). Constraints beyond type/enum may not be sampler-enforced by every provider; the admission door enforces them all, and a violation returns the taught refusal (see refusal_result). Model e5a69f8b2bdf6c04e7cd0f9a9f348aa2aa1deca79b9ab2d63266915c37aaad67; convention 3c6412aa9b70498e192b85e23cff7f3b5cb33ac797b76028e70fbd0e3526f71e.",
  "digest_format": {
    "$comment": "Every digest on the wire is the running system's spelling: the literal prefix sha256: followed by exactly 64 lowercase hexadecimal characters. The model's own identity labels are short ordinals and are a modelling convenience, never a wire spelling.",
    "type": "string",
    "pattern": "^sha256:[0-9a-f]{64}$"
  },
  "tools": [
    {
      "name": "kernel_declare",
      "description": "Mint an immutable value through the certifier (license: c7_pin_well_founded -- references resolve only to already-admitted digests). The value's canonical bytes become its digest, its name forever. There is no update: revision is a successor declaration pinning its predecessor.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["kind", "value", "writ_digest"],
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["schema", "program", "policy", "capability", "lane", "algebra", "index", "resource", "ontology", "schedule", "template", "language"],
            "description": "The declaration kind the minted digest is branded with."
          },
          "value": {
            "type": "string",
            "description": "The value's canonical bytes. One canonical form exists; excess or non-canonical bytes refuse."
          },
          "writ_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "Digest of the policy value this declaration acts under. Every referent inside value must lie in this writ's pinned universe (refusal: off-writ-referent)."
          }
        }
      }
    },
    {
      "name": "kernel_resolve",
      "description": "Digest to value, verify-on-read (license: content addressing -- the decode re-derives the digest of what it fetched and refuses on mismatch). No anchor parameter exists: a digest names one value forever, so nothing an anchor could change. Head-relative reads are kernel_fold.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["kind", "digest"],
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["schema", "program", "policy", "capability", "lane", "algebra", "index", "resource", "ontology", "schedule", "template", "language"],
            "description": "The declaration kind the digest is branded with; a kind/brand mismatch refuses (cross-sort-identifier)."
          },
          "digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "The content address to resolve. Absence is retryable; a digest mismatch is structural."
          }
        }
      }
    },
    {
      "name": "kernel_emit",
      "description": "Attributed evidence onto a lane (license: f2_trace_invariant -- duplication and reordering of deliveries cannot move the folded state). Not a send: no point-to-point primitive exists; lanes mean, subjects route.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["lane_digest", "body"],
        "properties": {
          "lane_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "Digest of the declared evidence lane the envelope lands on."
          },
          "body": {
            "type": "string",
            "description": "The evidence body, canonical bytes. The envelope's message id is its digest, so redelivery is harmless."
          }
        }
      }
    },
    {
      "name": "kernel_join",
      "description": "Merge a contribution into a lattice cell (license: f1_cell_merge_aci and the join-semilattice package -- associative, commutative, idempotent, so arrival order and duplication cannot matter). No ordering parameter and no conflict strategy exist; idempotent join leaves nothing to choose (refusal: last-writer-wins, machine-applicable repair).",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["cell_digest", "contribution"],
        "properties": {
          "cell_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "Digest of the declared cell resource whose ACI algebra governs the merge."
          },
          "contribution": {
            "type": "string",
            "description": "The contribution value, canonical bytes."
          }
        }
      }
    },
    {
      "name": "kernel_fold",
      "description": "Read reduced state at a checkpoint (license: f3_resume_exact for resumption; f11_query_deterministic -- the answer is a function of support and query alone). Every head-relative read is this tool: search, views, rosters, audits. The anchor names the position the answer is true at; it is never wrong later, only earlier.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["reduction_digest", "lane_digest", "shard", "anchor_floor", "anchor_state", "anchor_head", "query"],
        "properties": {
          "reduction_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "Digest of the declared reduction (kind: index) being read."
          },
          "lane_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "The lane whose partition the anchor coordinates name."
          },
          "shard": {
            "type": "integer",
            "minimum": 0,
            "description": "The partition within the lane. Positions mean nothing across partitions."
          },
          "anchor_floor": {
            "type": "integer",
            "minimum": 0,
            "description": "The checkpoint floor position, within this partition only."
          },
          "anchor_state": {
            "type": "string",
            "description": "The state label at the floor, as the anchor fact recorded it."
          },
          "anchor_head": {
            "type": "integer",
            "minimum": 0,
            "description": "The journal head the anchor observed, within this partition only."
          },
          "query": {
            "type": "string",
            "description": "The question, as declared data. A view passes a constant; any seed lives inside this value (refusal: ambient-query-input)."
          }
        }
      }
    },
    {
      "name": "kernel_decide",
      "description": "The fenced one-winner commit (license: at_most_one_landed_commit over monotone tokens -- at most one outcome lands per register). The one priced act. At most one landed outcome is not at most one external side effect.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["register_digest", "token_fence", "outcome"],
        "properties": {
          "register_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "The work declaration digest that keys the register. No anonymous decision exists."
          },
          "token_fence": {
            "type": "integer",
            "minimum": 0,
            "description": "The fencing token, meaningful at this register only. Committing without it, or across registers, refuses (unfenced-decide, cross-sort-identifier)."
          },
          "outcome": {
            "type": "string",
            "description": "The outcome value; the door decodes it against the capability's declared output schema."
          }
        }
      }
    },
    {
      "name": "kernel_trigger",
      "description": "Deposit a standing monotone reaction (license: f10_stability -- a predicate that holds at a state holds at every grown state; an enabled firing never un-fires). Five productions exist; absence, negation, and deadline are unspellable (refusal: absence-trigger).",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["production", "declaration_digest"],
        "properties": {
          "production": {
            "type": "string",
            "enum": ["evidence-appears", "cell-reaches", "hole-reaches", "outcome-landed", "head-advanced-past"],
            "description": "Which of the five monotone productions. Each uses its own slots below: evidence-appears uses lane_digest+pattern; cell-reaches uses cell_digest+threshold; hole-reaches uses hole+stage; outcome-landed uses register_digest; head-advanced-past uses lane_digest+shard+position."
          },
          "declaration_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "The declaration a firing hints at. Its landed claim dedups at the register."
          },
          "lane_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "evidence-appears, head-advanced-past."
          },
          "pattern": {
            "type": "string",
            "description": "evidence-appears: the observation pattern to await."
          },
          "cell_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "cell-reaches."
          },
          "threshold": {
            "type": "string",
            "description": "cell-reaches: the lattice threshold to reach at least."
          },
          "hole": {
            "type": "integer",
            "minimum": 0,
            "description": "hole-reaches."
          },
          "stage": {
            "type": "string",
            "enum": ["opened", "filled", "disputed", "decided", "sealed"],
            "description": "hole-reaches: the stage to reach at least."
          },
          "register_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "outcome-landed."
          },
          "shard": {
            "type": "integer",
            "minimum": 0,
            "description": "head-advanced-past."
          },
          "position": {
            "type": "integer",
            "minimum": 0,
            "description": "head-advanced-past: fire once the head passes this position (within this partition only)."
          }
        }
      }
    },
    {
      "name": "kernel_spawn",
      "description": "Derive a narrower writ (license: f9_policy_meet_semilattice and f9_tree_attenuation -- the child is the meet of parent and request, and every descendant stays under the root grant). An escalating request is clamped, not refused.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "required": ["parent_writ_digest", "request_writ_digest"],
        "properties": {
          "parent_writ_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "The authority being attenuated."
          },
          "request_writ_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$",
            "description": "The requested authority; the child is parent meet request."
          }
        }
      }
    }
  ],
  "refusal_result": {
    "$comment": "Every tool returns either its admitted result or this refusal shape. Refusal parity: the door never refuses without the law it defends and the taught repair; applicability marks whether the repair is a mechanical rewrite of the refused call (machine-applicable) or needs information the call does not carry (advisory).",
    "type": "object",
    "additionalProperties": false,
    "required": ["reason", "law", "repair", "applicability"],
    "properties": {
      "reason": {
        "type": "string",
        "enum": ["clock-read", "absence-trigger", "unfenced-decide", "last-writer-wins", "unverified-read", "cross-sort-identifier", "minted-identifier", "ambient-query-input", "forward-reference", "secret-carrier", "absence-claim", "past-mutation", "off-writ-referent", "closure-introspection", "anchored-resolve", "unfilled-hole"]
      },
      "law": {
        "type": "string",
        "description": "The law this refusal defends, by its real name."
      },
      "repair": {
        "type": "string",
        "description": "The legal next move, taught as data."
      },
      "applicability": {
        "type": "string",
        "enum": ["machine-applicable", "advisory"]
      }
    }
  }
}
