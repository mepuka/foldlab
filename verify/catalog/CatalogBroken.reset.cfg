\* Negative control 4: ResettingMirror — a replica loses its verified
\* prefix (ADR-0009's rejected alternative: history renumbered/dropped).
\* No fact is ever WRONG — LagIsAbsenceNeverWrongData still holds and is
\* checked to prove the two laws independent — but evidence un-resolves:
\* TLC MUST report a violation of ResolutionMonotonicity.
SPECIFICATION Spec
CONSTANTS
  NumDaemons = 2
  NumCreators = 2
  NumVals = 3
  DataCap = 2
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = TRUE
INVARIANTS TypeOK LagIsAbsenceNeverWrongData
PROPERTIES ResolutionMonotonicity
