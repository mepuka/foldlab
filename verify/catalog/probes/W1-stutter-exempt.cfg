\* BREAKER PROBE W1. False bridge on CreateAtomic's RESOLVING branch.
\* Cap2 bounds for speed. A CLEAN verdict is the finding: a knowingly
\* false obligation about that branch passed, so AtomicRefinement's
\* resolving-create clause is discharged by the [_]_vars subscript, not
\* by any check.
SPECIFICATION WireSpec
CONSTANTS
  NumDaemons = 2
  NumCreators = 2
  NumVals = 2
  DataCap = 1
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = FALSE
  BrokenAtomicBridge = FALSE
PROPERTIES StutterExemptProbe
