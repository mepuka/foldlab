\* BREAKER PROBE X4 — NEGATIVE CONTROL for X3, and the demonstration that
\* proposal 2 is STRICTLY stronger than NoAdmissionOnFaith. Under
\* ResettingMirror a daemon's resolvable set shrinks while committed
\* evidence does not.
\* AdmittedStaysResolvableAtD MUST be violated here.
\* Paired with X5, which checks NoAdmissionOnFaith over the same
\* behaviours and must stay CLEAN.
SPECIFICATION Spec
CONSTANTS
  NumDaemons = 2
  NumCreators = 2
  NumVals = 2
  DataCap = 1
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = TRUE
  BrokenAtomicBridge = FALSE
INVARIANTS AdmittedStaysResolvableAtD
