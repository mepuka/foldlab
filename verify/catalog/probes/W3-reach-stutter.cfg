\* BREAKER PROBE W3 — REACHABILITY WITNESS for W1. ReachStutter denies
\* that any reachable state enables CreateAtomic's resolving branch. It
\* MUST be violated; the counterexample is the proof that W1's antecedent
\* is satisfiable and W1's clean verdict is not vacuous-by-emptiness.
SPECIFICATION WireSpec
CONSTANTS
  NumDaemons = 2
  NumCreators = 2
  NumVals = 2
  DataCap = 1
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = FALSE
  BrokenAtomicBridge = FALSE
INVARIANTS ReachStutter
