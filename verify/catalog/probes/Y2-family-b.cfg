\* BREAKER PROBE Y2. Does IndInv exclude family (b) — a busy creator whose
\* remembered expected position runs past its own daemon's journal?
\* CLEAN = excluded outright. Paired with Y3, which must be VIOLATED.
CONSTANTS
  NumDaemons = 2
  NumCreators = 1
  NumVals = 2
  DataCap = 1
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = FALSE
INIT FamilyB
NEXT NoStep
INVARIANTS NotIndInv
