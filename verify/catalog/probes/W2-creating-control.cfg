\* BREAKER PROBE W2 — POSITIVE CONTROL for W1. Same false bridge, but on
\* CreateAtomic's CREATING (non-stuttering) branch. MUST be violated; if
\* it is not, the probe harness cannot fail and W1 proves nothing.
SPECIFICATION WireSpec
CONSTANTS
  NumDaemons = 2
  NumCreators = 2
  NumVals = 2
  DataCap = 1
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = FALSE
  BrokenAtomicBridge = FALSE
PROPERTIES CreatingProbe
