\* BREAKER PROBE X2 — NEGATIVE CONTROL for X1. The same obligation with a
\* CreateBeginResult that fails to stutter on a resolvable digest: exactly
\* the defect the [_]_vars subscript hides in AtomicRefinement.
\* MUST be violated. This is the direct comparison with probe W1, which
\* left the identical defect class undetected.
SPECIFICATION WireSpec
CONSTANTS
  NumDaemons = 2
  NumCreators = 2
  NumVals = 2
  DataCap = 1
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = FALSE
  BrokenAtomicBridge = FALSE
INVARIANTS ResolvingCreateAgreesBroken
