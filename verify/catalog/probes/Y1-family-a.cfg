\* BREAKER PROBE Y1. Does IndInv exclude family (a) — a mirror holding a
\* fact its origin's catalog lacks at that position?
\* Checked as INVARIANT ~IndInv over the family. CLEAN = every state in the
\* family falsifies IndInv, i.e. the family is excluded outright and no
\* counterexample-to-induction can live there.
\* Paired with Y3 (positive control), which must be VIOLATED.
CONSTANTS
  NumDaemons = 2
  NumCreators = 1
  NumVals = 2
  DataCap = 1
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = FALSE
INIT FamilyA
NEXT NoStep
INVARIANTS NotIndInv
