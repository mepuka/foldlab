\* R3 negative control: the same induction obligations with W4's
\* resolve-before-admit guard deliberately disabled.  Action safety MUST
\* be refuted; a green result means the proof harness cannot fail.
CONSTANTS
  NumDaemons = 2
  NumCreators = 2
  NumVals = 3
  DataCap = 0
  BlindIngress = TRUE
  ForgedMirror = FALSE
  AssertedIdentity = FALSE
  ResettingMirror = FALSE
INIT Init
NEXT Next
