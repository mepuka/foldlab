\* Negative control 3: AssertedIdentity — W1 dropped, the daemon commits
\* a creator-asserted identity without deriving it from the bytes.  TLC
\* MUST report a violation of Convergence: same value, two identities —
\* the assigned-correlation mistake the strictness law exists to refuse.
SPECIFICATION Spec
CONSTANTS
  NumDaemons = 2
  NumCreators = 2
  NumVals = 3
  DataCap = 2
  BlindIngress = FALSE
  ForgedMirror = FALSE
  AssertedIdentity = TRUE
  ResettingMirror = FALSE
INVARIANTS TypeOK Convergence
