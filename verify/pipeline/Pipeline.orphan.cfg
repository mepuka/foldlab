\* Finding control: a crash between fact and bridge leaves an orphan fact
\* (catalog.go:232-236 residual, audit §5.3). MUST be refuted on NoOrphanFact —
\* the committed trace is the finding, not a defect in the spec.
SPECIFICATION Spec
CONSTANT
  Rule = "snapshot"
INVARIANT
  NoOrphanFact
