-- TOMBSTONE. Nothing here is owed.
--
-- This file held sorry-bearing copies of three round trips parked out
-- of library/cas/Cas/Lang/Defun.lean on 2026-08-28 (the C4 rollback),
-- with a header directing a reader to repair and restore them. All
-- three are PROVED and LANDED in Cas/Lang/Defun.lean as of fb83712b:
--
--   readPIn_encodePIn      the operand round trip
--   readPRef_encodePRef    the typed-reference round trip
--   decodeLine_encodeLine  the code-point round trip
--
-- Un-parked 2026-08-29 by staging the proofs against abstract byte
-- strings, which is what retired the kernel-memory exhaustion that
-- forced the rollback. The exactness lemmas (readPIn_exact,
-- readPRef_exact, readLine_exact, decodeLine_exact) and the
-- table-level decoder (decodeProg, decodeProg_encodeProg) landed on
-- the same day above them.
--
-- The original text is retired: read the theorems in
-- library/cas/Cas/Lang/Defun.lean, which is the only place they are
-- stated, and where `lake --wfail build` checks them. A copy carrying
-- `sorry` beside a landed theorem is a claim-audit hazard, so this
-- file keeps no code at all.
