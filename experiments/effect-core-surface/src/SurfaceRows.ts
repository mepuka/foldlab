import type { Effect } from "effect"

/**
 * Empty scaffold for canonical source-surface rows.
 *
 * Logic enters only after the corresponding Effect Core packet slice and
 * generated input/output schemas are frozen.
 */
export {}
