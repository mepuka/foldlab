# Cure brief — Rev findings on the float-drop / hygiene-gates branch

Status: dispatchable to the Eng seat that owns
`agent/codex/kernel-hygiene-gates`; cure commits land on that branch
and the repair cites the Rev seat's report per house law. Authority:
operator rulings 5–6 (2026-08-17, grill record "Post-sweep rulings")
plus the ratified findings dispositions. Evidence, probe transcripts,
and acceptance vectors:
`docs/research/2026-08-16-review-float-hygiene-branch.md`. The
reviewer's clean detached worktree at
`C:/Users/kokok/Dev/foldlab-rev-hyg` reproduces every probe without a
fresh toolchain warm-up.

The DEV-670 generation barrier holds until this cure merges — the
corpus generates against the CURED grammar, not the wedge.

## C1 — narrow literal scalars to integers (F1, blocker; ruling 5)

`{"k":"literal"}` admits string | integral number | bool | null.
Reuse the exact integrality bound at value_check.go:45
(`Trunc(n) == n && |n| <= 9007199254740991`) — factor a shared
predicate rather than restating it. Update SPEC.md:72's grammar line
(the SPEC.md edit is authorized by this brief; record the authority
exception, see C8b). Negative vectors committed: `5e-324`, `0.1`,
`1e21`, `1e-7` as literal values each refused with the named
refusal; the reviewer's executed probes are the acceptance set.
Sweep fixtures for any existing non-integer literal (the survey
found none live, but the cure re-verifies) and regenerate through
the committed generators only.

## C2 — record opaque's law (F2; ruling 6)

No admission change. Record the ruled law where the contract lives
(CONTRACT.md and/or SPEC.md prose plus a DECISIONS entry): opaque
payloads are uninterpreted canonical bytes; identity is byte
equality by definition; the kernel never parses opaque; canonicity
of arriving opaque payloads is the shell's JCS seam. The REF-2a spec
and the ladder's trusted base already carry the coordinator-side
consequences.

## C3 — the three missing negative controls (F3)

`@[extern]`-allowlist, `partial`, and `sorry` checks each gain a
committed control refuted on exactly its own check, traces
committed, orphaned from the build like the two existing controls.
VERIFICATION.md's five-check claim becomes true rather than
aspirational.

## C4 — widen the panic-free gate (F4)

The gate must catch the Inhabited-default channel, not the token:
add bare `panic` (word-boundary), the bang-accessor family (curate
the list from Lean core — `.head!`, `.get!`, `.toNat!` and kin —
recorded in DECISIONS with the curation rule), and `unsafe`.
Consider `native_decide` and `noncomputable` while there; decide and
record. One negative control per new token class. The reviewer's
probes H (bare `panic`), I (`List.head!`), and K (`unsafe def`) are
the acceptance vectors: each must now exit 1.

## C5 — the generator joins the kernel-bound roster (F5)

Brief 22's premise ("hardens the corpus generator the estate already
trusts") becomes true: widen the roster to `Oracle/` and `Main.lean`.
Fix `Main.lean:86`'s `n.toNat!` (explicit parse with a usage
refusal). `partial def serve` (Main.lean:68) gets a named exception
with its reason — a non-terminating daemon loop is not a
value-defaulting channel — via the same allowlist mechanism as
`@[extern]` (per-site, reason recorded, operator-ratified), never a
silent roster carve-out.

## C6 — sessions.json becomes reproducible (F6)

Commit its generator (extend `cmd/wirefix` or a sibling), add the
`_provenance` line naming the generation command, and land the
regenerated bytes as THE bytes — eliminating the hand-edited
cosmetic churn by making the file machine-owned like its four
siblings. CONTRACT.md:395's "mechanically recomputed" claim is then
backed by a committed script, and the regeneration byte-diff joins
the gate that already covers the wirefix four.

## C7 — the survey lands in the tree (F7)

Commit the 16-site restatement survey (touched / checked-clean per
brief 21 §1), verified against the reviewer's independently built
table in the report. Any disagreement with that table is a finding,
not a merge.

## C8 — minors (F8–F12)

a. Widen `float_leaf_test.go`'s grep guard toward the 16-site list,
   or record the justified subset.
b. Record the SPEC.md-edit authority exception (brief-directed edits
   under ruling 2/5) so the next executor does not inherit an
   absolute law with a merged counterexample.
c. Correct verify/AGENTS.md:53's stale counts to the actual roster
   and control numbers at land time.
d. Record the session-grammar-digest hard-cut consequence in
   DECISIONS (pre-cut flb.session.v0 journals unopenable; bounded by
   "tracer data is disposable").

## Acceptance (mechanical)

- `bun run gates`, `bash verify/moves/run.sh`, `bash
  verify/ir/run.sh` all green on the cured branch tip.
- The review's probe battery re-run: every previously-caught plant
  still exits 1; probes H, I, K now exit 1; comments still exit 0.
- Literal negative vectors refused by name; `5e-324` dead.
- All regenerable fixtures regenerate byte-identically from
  committed commands, sessions.json included.
- Survey in tree; VERIFICATION.md claims true; DECISIONS entries per
  house format.

Seats: Eng cures; the SAME Rev seat re-reviews (repair mentions the
findings report); operator ratifies and merges. The issue body is
this brief plus the findings report it cites.
