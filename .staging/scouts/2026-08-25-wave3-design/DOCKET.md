# Wave-3 grill docket — items with recommended rulings

**Status: staged docket, 2026-08-25 — no claim gate satisfied. Recommendations are the
coordinator's; every ruling is the operator's** (PROCEDURE Phase 2). Evidence:
`SYNTHESIS.md`, `DOMAIN-LAYOUT.md` (D1–D6), and the six reader reports in this
directory. One docket, deliberately composed (PROCEDURE §3.2). Items are
dependency-ordered; each carries the recommendation and its one-line basis.

## Block I — layout (D1–D6)

| # | Item | Recommendation | Basis |
|---|---|---|---|
| 1 | D1 authority map + registry rows | **Option (a)**: `RULINGS.md` sole ruling authority; adopt the four replacement/new registry rows as drafted in the packet | dockets will produce rulings faster than the audit series can keep findable; one home ends the §7/§18/§8 scatter |
| 2 | D2 the two interfaces | **Adopt as drafted**: `Reachable`/`Admissible` pair in the model; `runVerb` decision module + persistence seam in the shell; three-way error ownership | operator's corrected architecture; `Admissible` names "what a client may rely on after `check`" |
| 3 | D3 `Admissible` ownership | **`Admission` owns** the judgment + `admissibleReport`; `Graph` analysis-only; helpers internal | one idea, one module; seat counts follow design |
| 4 | D4 status-generation source | **Prose/trace/anti-claim normative in STORE-MODEL §6; status stamped from `LEDGER.md`** via one extractor join; interim hand-flag banner | no second maintained copy of any promise; extractor stays mechanical-only |
| 5 | D5 naming module | **Defer extraction**; rule item 14 (names relation/plane) first; extract only on shown independent callers, change pressure, or adapters | F-39 shows inconsistency, not an interface |
| 6 | D6 migration order | **Adopt the six steps as drafted** (rule → CONTEXT/registry → repairs → ledger → reorganize on generated statuses → retire review) | `Admissible` never precedes its owning context; no hand-copied promise table ever exists |

## Block II — the typing plane (family 1)

| # | Item | Recommendation | Basis |
|---|---|---|---|
| 7 | M17 repair route | **Option (i-a)**: `putS` premise `WFS (canonS s)`; `putE` premises `Conforms env (canonS s) (canonV v)` and `dupFreeV (canonV v)`. B4 retired. M17′ stated as owed in the same amendment | zero proved theorems consume the old premises; three proofs touched, two shorten; M17 follows by construction without `H`-injectivity (R-D §3.1) |
| 8 | STORE-MODEL §5's `s` | **Read `s` as the resolved schema** → the schema half is an F-22-class transcription correction; mint the FINDINGS row | §5's first bullet binds `s` to `resolve_S`, which returns `canonS s` by L-faithful (R-D §1.1) |
| 9 | A-3 record reversal | **Reverse it**: value-plane duplicate-freedom becomes `Reachable` premises (the two clauses of item 7 + item 10) | four post-dating items: witnesses built inside the model; the delegated check doesn't exist and its mechanism is defeated; A-4 widened the route; the dedup obligation is vacuous exactly there (R-D §6.2) |
| 10 | F-26 repair + A-6 order | **`dupFreeS (.lit v) := dupFreeV v` ships with A-6; order A-6+F-26 (window A) → `Reachable`/`WFS` amendment (window B) → M17 seat** | F-26 is load-bearing for F-28 independently of A-6; the M17 seat never unfolds `canonS`, so it runs once if it runs last (R-D §8) |
| 11 | Q12 / F-36 | **Posture (a)**: the drafted anti-claim sentence + an Anti-claim column on §6's coupling table. Reject (b) and `anyOfOnly` | (a) costs zero obligations; (b) is blocked by strict positivity; `anyOfOnly` would REJECT-v1 a construct MAPPING row 26 already admits (R-D §7) |

## Block III — the boundary (family 2) and canon

| # | Item | Recommendation | Basis |
|---|---|---|---|
| 12 | Family-2 package | **Adopt R-C's package whole**: `wfsB` + iff in E2 (per D3, inside `Admission`); boundary and scan call it; Kahn's + order emission (option (b) `order` verb + (c) violation line); SH5 narrowed to `Admissible`; the twelve text changes | all three conjuncts exist and are called by nothing; the boundary check establishes `Admissible.admitted` with the decoded carrier as witness (R-C §1) |
| 13 | Check ordering | **WFS before canonicity, on BOTH sides** (PUT and scan align on the PUT order) | `ObligationCanonIdempotent` is conditional on `dupFreeS` — the byte-compare currently runs outside its own hypothesis; alignment costs one scan line and buys one mental model (R-C §3) |
| 14 | Names (F-39) | **Mint host-relation neutrality; rule the model authoritative; option 2** (hex-encoded name files) with the 64-char name cap and a names listing verb | closes all four hazards by construction, uniform with the objects plane's existing hex discipline; option 3 invents a fourth serialization format plus a concurrency question; option 4 refuses both dev hosts (R-C §5.3) |
| 15 | F-42 | **Admit `symlinkMetadata` single-purpose; regular-files-only read rule; typed `StoreFault` with deterministic render; three-way exit codes reachable; G-S5 gate leg** (no `Metadata` timestamps) | one primitive kills all four hazards; the exit contract already exists and is unreachable; the struct smuggles a clock unless gated (R-C §4) |
| 16 | Check-downgrade posture | **An enacted boundary check may be weakened only by a ruled amendment citing an F-number — never inside a seat, never to un-break a consumer** | Unison's hard error was softened to an ignored warning within ~24 hours (R-A U-6) |
| 17 | Spelling rule | **Option (b) as `WFS` conjuncts**: the clause table incl. `usesBinderB`; `canonS` stays constructor-preserving permanently; completeness carries an unchecked-claim marker; union dedup mode-gated (SP-3); carrier narrowing reserved for the R-2 window | canonicalization can absorb none of the ten without widening ≈ or coarsening it below the subject; both prior single-spelling rulings were rejections; near-zero proof rework, zero address movement (R-B §9) |
| 18 | F-35 | **Mint the model clause**: `litNarrowB` as a `WFS` conjunct, same window | third model-accepts/boundary-rejects instance; the boundary rule is load-bearing for WF2 |
| 19 | `canonFields` comparison flip | **Rule yes; sequence after the boundary `WFS` check is live** | one comparison retires F-12/F-26/F-41 and makes the S1 obligations unconditional; alone it makes F-40 worse (R-E P10/P11) |

## Block IV — instruments, amortization, pins

| # | Item | Recommendation | Basis |
|---|---|---|---|
| 20 | Harness | **Gain the `(place …)` primitive family; commit the defect scripts now and flip them at amendment** | three of the six family-2 pieces are otherwise untestable; the flip is the amendment record under the §5 ratchet (R-C §2.7) |
| 21 | SH5′ amortization | **Defer the ruling until a measurement taken after the Θ(n²) drop fix lands; adopt M-INV-1..4 now as the frame; script 28-manifest-lying is the acceptance test** | the drop fix halves the cost being amortized; the estate's own record holds a measured 6× caching backfire (R-C §6, R-E) |
| 22 | Pins | **Pin all five**: M19 on `Admissible`; M10 with address-node vocabulary + `ObligationM10_rank`; M11-commutation up to find-extensionality with `reachable_keys_nodup`; `version_byte_separates`; the intra-kind honest form ("injective except on the characterised set") | refuter-supplied forms exist for the first three; the last two are cheap and close Unison-shaped assertion gaps (R-A U-16/U-9) |

## Block V — dispatch now, no ruling needed

| # | Item | Act |
|---|---|---|
| 23 | `.gitattributes` `* -text` under the store root | one file; the estate has already shipped one CRLF-corrupted digest record |
| 24 | F-45 script count | one-line doc fix, both homes |
| 25 | Θ(n²) `List.drop` fix in `fips202` absorb | ~2 lines, representation-preserving, halves the 2 MB digest cost; KATs re-run |

---

*End. Staged docket; recommendations only. Rulings are the operator's.*
