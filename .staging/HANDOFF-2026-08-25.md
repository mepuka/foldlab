# Handoff — 2026-08-25 (spine design-document session)

For a fresh agent continuing this work. Location note: written into `.staging/` (gitignored,
survives OS temp cleanup) at the operator's request instead of the OS temp directory.
Everything here is a pointer; do not re-derive what a referenced artifact already records.

## Where the program stands

- **fips202 is promoted and fully green.** `formal/fips202` on `main`: 67 theorems, FIPS 202
  spec + executable SHA3-512 + proved refinement (`sha3_512_bridge`), axioms within
  `[propext, Classical.choice, Quot.sound]`, dual-host battery + bundled `leanchecker` clean,
  and CI run 32805531256 green on ubuntu/macOS/Windows. Read its `README.md` and
  `PROVENANCE.md` for scoped claims; `TOOLING-NOTES.md` for the seven gate-tooling edge cases
  (case 7: a clean `git status` is not a clean state; case-6 companion: commit minting is a
  gated step, never a proof seat's side effect).
- **The next artifact is ratified**: A1 (machine-checked exhibit of Unison's #3509 collision)
  + A2 (corrected tokenizer + injectivity) as ONE contract, consuming `formal/fips202` as a
  Lake dependency. First-hand evidence chain, pinned to the clone at
  `.reference/clones/unison` @ 84b95a6:
  `unison-hashing-v2/src/Unison/Hashing/V2/Tokenizable.hs:122-137` (serializer not
  prefix-free/type-discriminated), `.../DataDeclaration.hs:27,40-43,105-128` (hashing IR has
  no effect/data constructor), `parser-typechecker/src/Unison/Hashing/V2/Convert.hs:247-267`
  (Either stripped pre-hash, retagged after).
- **Scratch Lake project** `.staging/unison-fragment/` (v4.33.1, builds green):
  `Fragment/Stream.lean` holds the Token mirror, `v2_stream_not_injective` (proved), and
  obligations F1/F2/F3 as named Props — statements only, deliberately nothing frozen.

## The next deliverable: the language design document

The operator designed the spine's core in-session, four ratified-as-input theses, all
captured with decodes in **`.staging/explore/spine-design-inputs.md`** — this file is the
design document's skeleton and the single most important input:

1. End-shape-first programming (typed holes; the ledger sketch).
2. Semantic discriminators for structural collisions (kills #3509-class bugs AND Unison's
   random-GUID non-derivability; theorem ladder D1 admission / D2 scoped injectivity /
   D3 source-derivability; the discriminator is the ONE identity-bearing string).
3. v1 domain: declarative JSON combiner over git-storable directories; policy totality at the
   parent scope; dogfood target = replacing `build_ledger.py`.
4. Scope shape: git tree default; generalization = inductive shapes gated by strict
   positivity (the host kernel's own check); fold/initiality gives determinism+termination
   generically; v1 fixes the tree as the only scope shape.

Open rulings the doc must surface (marked in that file's §5 and in-session): the v1
constructor list (candidate frame: atom, object, array, reference-by-hash, combinator
application, kind annotation — JSON-shaped, NOT lambda calculus), token-vs-scoped-rule
precedence, whether a directory IS a scope (policy file lives in the directory it governs,
`.gitignore`-style — operator leaned yes, unruled), address version placement, the
@[extern]/@[implemented_by] attribute-audit gate.

Evidence base (all `.staging/explore/`, all landed 2026-08-24/25): `pl-theory-curriculum.md`
(operator's Lean-verified theory course), `syntax-grammar-design.md` (+ verified-parser gap:
none exists in Lean 4), `lean-host-capabilities.md` (measured host numbers; the proof-seam
@[extern] finding), `hash-db-anatomy.md` (carrier→encoding→address frame + straw store),
`concrete-absorb-path.md` (9-week operator absorb plan), `language-design-case-studies.md`
(verified receipts incl. Part II McCarthy→Harper→Krishnaswami→Unison chain),
`concrete-spine-feasibility.md`, `spine-design-inputs.md`.

Document style: the operator is learning PL theory deliberately — write the design doc so each
section OPENS with the decision it needs, then the theory pointer, then the evidence. The
operator rules; the doc proposes. Outsider-legible prose, glossed jargon.

## Parked / owed (do not silently drop)

- **Paper-ledger landing**: RATIFIED (all recommendations) — verdicts in the header block of
  `.staging/explore/papers-registration-proposal.md`. The registration worktree session went
  unreachable before live relay; SINGLE-WRITER RULE recorded there — announce before touching
  `CLUSTERS`/`titles.json`. After landing: clear warning blocks in `hash-db-anatomy.md` §9.3
  and `lean-host-capabilities.md` finding 24. Why3 + Creusot PDFs need a human-browser fetch
  (HAL anti-bot); URLs in the absorb appendix.
- **Zulip post for fips202**: drafted-on-request, operator posts by their own hand; scoped
  claim only ("first in Lean 4 for this statement shape, to our knowledge"). D6 upstream
  filings remain HELD until the operator says so — nothing public touches the Unison work.
- **check-proofs re-adjudication** (Concrete): the old 0/11 ran on WSL2 where mktemp exists,
  so the absorb report's "harness fault" theory is unproven; the unbound-fingerprint ground
  stands. Open re-check, low priority.
- **Codex overstep record**: commit 0ae41a2 was minted by a codex seat past its fence
  (kept, operator-approved content, superseded layout removed in 56ff77b). Stash of its
  divergent files in session scratchpad (may be gone); git history has everything.

## Harness rules for this machine (hard-won, do not relearn)

- PowerShell tool ONLY (Bash tool hangs; claude-code#10181). Long jobs `run_in_background`
  from the start. Full-log-to-file then Select-String — exit codes and truncated output lie.
- liteparse for PDFs, always `-o`; tables via `screenshot --target-pages` + visual read.
- Lean proof loops: see `formal/fips202/CODEX-HANDOFF.md` for the house lesson book (targeted
  `simp only`, vget-style instance pinning, statement pins, axiom allowlist, no
  native_decide/bv_decide, zero warnings).
- Dual-host gate: anything that lands gets re-checked on the Mac (`ssh mac`), absolutely
  clean, plus name-for-name axiom diff. Memory files under
  `~/.claude/projects/C--Users-kokok-Dev-foldlab/memory/` carry the standing laws — read
  MEMORY.md first.

## Suggested skills

- `estate` — load before touching anything in the repo; binds grades, claims, provenance.
- `mattpocock-skills:grilling` — the design document is built to be grilled section by
  section; the operator asks for this by name ("grill").
- `technical-writing-voice` — for drafting the design document prose.
- `mattpocock-skills:domain-modeling` — when the design doc mints terms (discriminator,
  scope, kind, policy) they should enter the domain model deliberately.
- `lean4:lean4` — any work in `.staging/unison-fragment` or `formal/fips202`.
- `liteparse` — any PDF reading.
- `show-me` — the operator responds well to diagrams for the carrier→encoding→address
  layering; use for design-doc figures.
