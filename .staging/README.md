# Staging

Staged material: built things whose grade and publish-readiness are undecided.
Nothing in here is an artifact yet; promotion out of staging is a declared
transformation into `formal/` (gated) or the experiments tree.

The `.md` documents in here are TRACKED archival specs (operator
ruling 2026-08-29) — categorized, with the decision record, in
[docs/SPECS.md](../docs/SPECS.md). Corpora, caches, clones, and
`node_modules` stay local (see `.gitignore`).
