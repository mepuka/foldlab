# PDD-3 — References and recursion, slices 1-3 (Lane A, bounded)

> RULED 2026-08-30 (operator, on the builder's block): slice 2 grows
> the universe by TWO constructors — `Ast.reference (name : String)`
> (the edge into the references table) and `Ast.susp (thunk : Ast)`
> (the guard), matching Effect's pinned two-node spelling. The plan's
> one-constructor line (:127-131) is amended by this ruling — under
> one constructor the guardedness law is vacuous and its falsifier
> unconstructible (adequacy class fired; the spec was the bug). The
> §3-addendum theorem stands VERBATIM: every cycle passes through a
> `susp`. Neither constructor adds a sort.

CATEGORIES inductive-data, specification-design, termination,
           abstraction-modules, algebraic-laws
BRANCH     agent/opus-cc-mac/pdd-3

Dispatch under the proof-driven law: read
`.claude/skills/implement/SKILL.md`, `CONTRACT.md`,
`IMPLEMENTER.md`, and your categories' rows in `CATALOG.md` before
anything else. Wave-1 flow (operator-ruled): you are the builder —
packet first, implementation second, independent breaker afterward.

## The work

Lane A of `.staging/operational-structure/CORE-ABSTRACTIONS-PLAN.md`
(:110-166), SLICES 1-3 ONLY, plus its theorem obligations
(§3 addendum :919-938). Slices 4-5 (TS materialization, recursive
byte-gate fixture) are deliberately NOT in this ticket — they follow
after the break pass.

1. **Spelling probe** (read-only, first act): pin the exact rev-1
   JSON keys Effect emits for `Suspend`/`Reference` and the
   `references` table from the pinned source
   (node_modules/effect/src/SchemaRepresentation.ts, node
   interfaces around :329-460) in a probe test. The plan marks the
   spellings PENDING-VERIFICATION — nothing is written in Lean
   until the probe pins them.
2. **Carrier**: `Ast.susp (name : String)` (Cas/Schema/Ast.lean —
   universe today at :66-161 has no reference-into-table
   constructor); `WF` extension: name nonempty. The ADDRESS
   discipline is deliberately NOT in `WF` — it is the door's and
   materializer's question, per the ruling.
3. **Document plane**: references table decoded/emitted through the
   envelope (`ofRepresentationJson`/`toRepresentationJson`;
   envelope key lists read off the projection,
   Cas/Backend/Admission.lean:24-39); guardedness check at
   admission.

Theorems (packet laws): `references_guarded_decidable` — the
references table induces a finite edge relation; the admission
check decides "every cycle passes through a `susp`" by fuel-bounded
DFS with fuel = table size; decidability is the theorem, the check
IS the door. Round-trip extension per constructor (SelfCodec and
RepNormal grow by one case each; SM.md:169-174 names the ripple
list). Falsifiers: an unguarded cycle the door must refuse (the
witness table, as a counter-`example`/test), and a round-trip
witness per new case.

Deliberately NOT claimed (claim-scope line in the packet): El over
`susp` — no denotational adequacy for recursive codes in v1 (plan
HARD PARTS 2); cross-door recursive-value agreement bounded to
admission agreement.

## Castle requirements

- Packet at `library/cas/contracts/PDD-3.contract.md`, committed
  BEFORE any carrier code.
- Slice 3 moves the admission table: regenerate through the
  backend's own workflow (read `.claude/skills/backend-materialize`
  first) — byte gates regenerated by the emitters, never
  hand-edited.

## Fences

Must not touch: `El`'s denotation, `Cas/Backend/Mcp.lean`, the
checks layer, `library/effects/src/cas/Programs.ts`,
`library/effects/test/Programs.test.ts`, and every file the merge
branches carry (plan §0:40-78).

## Gates

`lake build` green; `mise run check:cas` fully green after
regeneration (schemas, addresses, verdicts, gate table — the
admission table moving is exactly what the gate is for). Commit on
your branch only, ticket key in every commit title, never merge.
