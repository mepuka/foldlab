# Ornamentation lane — coordination record

Status: coordination record, 2026-08-29 late (decision 33 + operator
role correction, same sitting). Everything here is pre-grade;
generated visuals and Paper output are conception evidence with no
trust contribution.

## Ownership (corrected)

Claude's frontend lane OWNS this directory and every deliverable in
it: ORNAMENTATION.md, PROOF-OBLIGATIONS.md, WASM-CANVAS.md, and any
further files it deems the work needs (grammar, color, assets,
interaction, density, artboards). Priorities, dispatch, and
ratification stay with the coordinator (foldlab-f7).

Codex serves in SUPPORT: guidance, deep research, technical
investigation, proof analysis, independent overwatch — on bounded,
non-overlapping tasks assigned by the coordinator when the lane
needs depth or an independent lens. Codex owns no files here and no
workstream runs parallel to the lane.

## Input on record (optional, pre-grade, no trust contribution)

A Codex synthesis proposed: one addressable ornament grammar (not a
CSS theme); MotifNode (canonical geometric DAG) + Ornament (semantic
placements) as candidate described kinds; three related families
(Prairie Ledger = order; Light Screen = boundaries/projection/lift;
Taliesin Bloom = memory/earned completion); precedence
foundation→datum→joint→cadence→sign→bloom→light/motion;
denotation-first with per-surface projections; raster never
authority. Two retained image studies sit at
~/.codex/generated_images/01a04f68-512d-7270-8b66-a56c6f3323e6/.
The frontend lane assesses this direction on the merits and absorbs
what survives into its own files.

## Law that binds every file here
- Decision 21 + addendum (foldkit; components speak the estate
  language; the component register is a gated manifest).
- Decision 23 (names are annotations never identity; new kinds yes,
  new sorts NO; the working-tag process for any tag).
- Docket Tier-2 item 17 / decision 28 (generated-viewer-default,
  authored-override). Generated viewers stay read-only and
  message-free.
- P4: no hand-maintained views of generated facts.
- Raster is never authority; the vector/denotation is.
- Decision 32(a): tier-0 projections are daemon-served, read-only.

## Operator decisions queued from this lane
1. Any new described kinds for ornament (MotifNode/Ornament or the
   lane's own shapes) — grill when the lane's grammar position lands.
2. An image-generation service — pre-grade design only; a build
   ruling only after refusal/drift gates are specified.
