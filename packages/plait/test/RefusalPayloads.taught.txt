# Generated by: bun run generate:refusal-payloads
# Authority: every object literal under packages/plait/src carrying a `law` field is a taught refusal payload; its kind, law, expected, and next texts are pinned here byte for byte, so an edit to any minting site's teaching reddens this wall.
packages/plait/src/carriage/Engine.ts#0
  kind "cataloged-value-absent"
  law "The engine carries an admitted act only at a binding its own declarations registered."
  expected <expression>
  next[0].subject "Engine.${}"
  next[0].note "Declare the carrier through this engine first, then retry the act."
packages/plait/src/carriage/Engine.ts#1
  kind "malformed-value"
  law "A program node executes only when its declaration and supplies complete one candidate sentence."
  expected "arguments and supplies that complete the generator's candidate form"
  next[0].subject "Engine.run"
  next[0].note "Supply the execution-time slot the declaration form cannot carry, or repair the node's arguments."
packages/plait/src/carriage/Engine.ts#2
  kind "malformed-value"
  law "A program executes only when its node list satisfies the model's admission relation."
  expected "newest-first nodes, every consumption already admitted, every name minted once"
  next[0].subject "Engine.run"
  next[0].note "Build the program through the builder, whose accumulator keeps the admission order."
packages/plait/src/internal/anchors.ts#0
  kind "anchor-transport-unavailable"
  law "Transport absence may be retried; anchor revision conflicts may not."
  expected "the pinned local NATS KV operation to be available"
  next[0].subject "Folds.deploy"
  next[0].note "A transport refusal leaves this anchor write's outcome unknown. Do not retry it in place: detach and redeploy - resumption reads the landed anchor back, and a retried CAS that already landed refuses as lost-anchor-cas by design."
packages/plait/src/internal/anchors.ts#1
  kind "malformed-anchor-state"
  law "An anchor and its content-addressed state must re-derive to the recorded canonical digests."
  expected <absent>
  next[0].subject "Folds.deploy"
  next[0].note "Restore the checkpoint fact and state bytes written only by the anchor adapter."
packages/plait/src/internal/anchors.ts#2
  kind "lost-anchor-cas"
  law "One live pump owns each fold partition; losing its anchor revision CAS is a fatal detach."
  expected "the still-current anchor revision"
  next[0].subject "Folds.deploy"
  next[0].note "Detach this pump; do not re-read and continue (${})."
packages/plait/src/internal/anchors.ts#3
  kind "anchor-absent"
  law "A deployed partition publishes its floor-zero anchor before returning its handle."
  expected "a deployed fold anchor"
  next[0].subject "Folds.deploy"
  next[0].note "Deploy the fold to create or resume its anchor."
packages/plait/src/internal/anchors.ts#4
  kind "anchor-substrate-shape"
  law "The anchor bucket is file-backed R=1 with 64 revisions, no age or size eviction, and no admin surface beyond it."
  expected "file/R=1/history=64/ttl=0/max_bytes=-1/direct=on, and no republish, subject transform, mirror-direct, atomic publish, message counter, compression, or value-size cap"
  next[0].subject "Folds.deploy"
  next[0].note "Restore the ruled flb-fab-anchor bucket shape, admin surface included, before deploying folds."
  next[0].body <expression>
packages/plait/src/internal/carriers.ts#0
  kind "mirrored-authority-carrier"
  law "An authority carrier is the sole origin of its facts: its stream imports nothing — no mirror, no sources."
  expected {"mirror":null,"sources":[]}
  next[0].subject <expression>
  next[0].note "Open this carrier against a stream that imports nothing, and declare a replica read-plane carrier for the mirrored copy instead: a mirror stores its origin's facts at its origin's sequence numbers and is locally read-only, so it can be read but never held as an authority (ADR-0009). Sources renumber and carry no journal at all."
  next[0].body {"mirror":null,"sources":[]}
packages/plait/src/internal/carriers.ts#1
  kind "expiring-authority-carrier"
  law "An authority carrier never expires a fact it carries: a fact the server deletes un-decides every decision that cited it."
  expected {"allow_msg_ttl":false}
  next[0].subject <expression>
  next[0].note "Open this carrier against a stream created with per-message TTL off — the server refuses to clear `allow_msg_ttl` once it is set, so an existing one is replaced, not edited — and carry material that is meant to expire on a carrier no decision reads."
  next[0].body {"allow_msg_ttl":false}
packages/plait/src/internal/cells.ts#0
  kind "cell-transport-unavailable"
  law "Transport absence may be retried; lattice-law refusals may not."
  expected "the pinned local NATS KV operation to be available"
  next <expression>
packages/plait/src/internal/cells.ts#1
  kind "invalid-cell-key"
  law "A cell name maps to one literal NATS KV key."
  expected "one non-empty token without dots, whitespace, or wildcards"
  next[0].subject "cell.read"
  next[0].note "Present the cell name as one literal KV token without dots, whitespace, or wildcards."
packages/plait/src/internal/cells.ts#2
  kind "malformed-cell-state"
  law "Cell state is the canonical array of holder-attributed observations."
  expected "[{ holder: Json, value: Json }, ...]"
  next[0].subject "cell.read"
  next[0].note "Restore the canonical observation array at this key; only cell merges write this bucket."
packages/plait/src/internal/cells.ts#3
  kind "cell-substrate-shape"
  law "The cell bucket is file-backed R=1 with one retained revision, no age or size eviction, and no admin surface beyond it."
  expected "file/R=1/history=1/ttl=0/max_bytes=-1/direct=on, and no republish, subject transform, mirror-direct, atomic publish, message counter, compression, or value-size cap"
  next[0].subject "bucket.ensure"
  next[0].note "Configure the cell bucket file-backed with one replica, one retained revision, no age or size eviction, and none of the admin surface beyond it."
  next[0].body <expression>
packages/plait/src/internal/cells.ts#4
  kind "cell-update-contended"
  law "A merge lands its delta or reports contention; a cell is never written by anything but a join."
  expected "one uncontended revision within the attempt bound"
  next[0].subject "cell.merge"
  next[0].note "Re-attempt the merge; the join is idempotent, so a repeated delta adds nothing twice."
packages/plait/src/internal/chaos.ts#0
  kind "chaos-transport-unavailable"
  law "The chaos schedule uses only the real durable-consumer protocol for redelivery."
  expected "the pinned local NATS consumer operation to be available"
  next[0].subject "plait chaos"
  next[0].note "Restore the consumer protocol and re-run the measurement against the same pinned span."
packages/plait/src/internal/chaos.ts#1
  kind "chaos-message-absent"
  law "A pinned chaos span must already be present before the schedule starts."
  expected "one message inside the pinned span"
  next[0].subject "plait chaos"
  next[0].note "Emit the complete pinned span, then re-run the measurement against that same span."
packages/plait/src/internal/chaos.ts#2
  kind "invalid-fold-state"
  law "Chaos measurements admit only the fold runtime's wire-grammar states."
  expected "one RFC 8785 wire value"
  next[0].subject "Algebra.declare"
  next[0].note "Declare a canonical wire-grammar initial state before running chaos measurements."
packages/plait/src/internal/chaos.ts#3
  kind "invalid-fold-declaration"
  law "Chaos pins one positive stream head for every declared partition before scheduling."
  expected "${} positive safe-integer heads"
  next[0].subject "plait chaos"
  next[0].note "Pin one positive stream head for each declared lane partition."
packages/plait/src/internal/folds.ts#0
  kind "fold-transport-unavailable"
  law "Transport absence may be retried; declared fold laws may not."
  expected "the pinned local NATS operation to be available"
  next[0].subject "Folds.deploy"
  next[0].note "Reconnect and redeploy; resumption re-attaches at floor + 1, and redelivery replaces anything left unacknowledged."
packages/plait/src/internal/folds.ts#1
  kind "invalid-fold-declaration"
  law "Fold checkpoint flow control is a positive bounded batch size."
  expected "a positive safe integer"
  next[0].subject "Folds.deploy"
  next[0].note "Choose checkpointEvery >= 1; it changes batching, not correctness."
packages/plait/src/internal/folds.ts#2
  kind "invalid-partition-key"
  law "A fold handle exposes only its declared partition anchors."
  expected "an integer from 0 through ${}"
  next[0].subject "FoldHandle.anchor"
  next[0].note "Inspect only a partition declared by this fold's lane."
packages/plait/src/internal/incarnations.ts#0
  kind "malformed-value"
  law "An incarnation chain is walked by predecessor digest and every step names an incarnation the history carries."
  expected <absent>
  next[0].subject "incarnation.chain"
  next[0].note "Walk the chain over a history carrying every incarnation the walk reaches, each named by its own canonical bytes."
packages/plait/src/internal/lanes.ts#0
  kind "lane-transport-unavailable"
  law "Transport absence may be retried; declared lane shape violations may not."
  expected "the pinned local NATS JetStream operation to be available"
  next[0].subject "Lane.emit"
  next[0].note "Reconnect and re-emit; F2 makes duplicate delivery harmless, and the envelope digest is the message id."
packages/plait/src/internal/lanes.ts#1
  kind "lane-substrate-shape"
  law "Each declared (lane, partition) has one exact, file-backed R=1 stream with no count, byte, or age eviction, and no admin surface beyond it."
  expected <expression>
  next[0].subject "Lane.emit"
  next[0].note "Restore the declared partition stream shape before admitting evidence."
packages/plait/src/internal/lanes.ts#2
  kind "payload-substrate-shape"
  law "The inline-body threshold of ${} bytes is pinned against a measured max_payload of ${}; a substrate advertising less cannot carry an emit at that threshold."
  expected <expression>
  next[0].subject "Lane.emit"
  next[0].note "Raise the server's max_payload to the pinned budget, or emit against a substrate that already advertises it; the threshold is not renegotiable per connection."
  next[0].body <expression>
packages/plait/src/internal/nats.ts#0
  kind "transport-unavailable"
  law "Transport absence may be retried; structural envelope evidence may not."
  expected "the pinned local NATS operation to be available"
  next <expression>
packages/plait/src/internal/nats.ts#1
  kind "substrate-shape"
  law "The commons control stream is file-backed R=1 and excludes partition evidence streams."
  expected <expression>
  next[0].subject "stream.ensure"
  next[0].note "Configure the file-backed fact/node control stream with one replica and the ruled subjects."
  next[0].body <expression>
packages/plait/src/internal/pump.ts#0
  kind "fold-transport-unavailable"
  law "Transport absence may be retried; consumer and checkpoint shape violations may not."
  expected "the pinned local NATS durable-consumer operation to be available"
  next[0].subject "Folds.deploy"
  next[0].note "Reconnect and redeploy; resumption re-attaches at floor + 1, and redelivery replaces anything left unacknowledged."
packages/plait/src/internal/pump.ts#1
  kind "consumer-substrate-shape"
  law "Every fold partition has one durable pull consumer with explicit ack and a bounded in-flight window."
  expected "explicit ack, one exact filter subject, max_ack_pending=${}"
  next[0].subject "Folds.deploy"
  next[0].note "Restore the declared durable pull-consumer shape before attaching the pump."
packages/plait/src/internal/pump.ts#2
  kind "lane-evidence-mismatch"
  law "A durable partition pump accepts only verified events addressed by its declared lane and partition key."
  expected <absent>
  next[0].subject "Lane.emit"
  next[0].note "Emit through the declared lane so identity, key, subject, and partition are derived together."
packages/plait/src/internal/pump.ts#3
  kind "fold-buffer-overflow"
  law "The position-addressed reorder buffer is bounded by the durable consumer's in-flight window."
  expected 256
  next[0].subject "Folds.deploy"
  next[0].note "Detach the pump and restore the declared max-ack-pending flow-control shape."
packages/plait/src/internal/refusals.ts#0
  kind "malformed-value"
  law "A decoded value satisfies its declared schema; a decoder that repairs its input names a different value."
  expected "one value admitted by the declared schema"
  next[0].subject "decode"
  next[0].note "Submit a value the declared schema admits; the reported issue names the refused path."
packages/plait/src/internal/registers.ts#0
  kind "register-transport-unavailable"
  law "Transport absence may be retried; fencing-law refusals may not."
  expected "the pinned local NATS KV operation to be available"
  next[0].subject "register.observe"
  next[0].note "Reconnect to the pinned server and observe the register: a transport refusal leaves this operation's outcome unknown, so read the landed holder, token, and outcome back before retrying it."
packages/plait/src/internal/registers.ts#1
  kind "incarnation-mismatch"
  law "A fencing token is honored only by the backing-stream incarnation that minted it."
  expected "a backing stream that reports its creation time"
  next[0].subject "register.open"
  next[0].note "Re-open the register and re-derive the fence from the live bucket; a reborn bucket's revisions restart, so every token minted under the destroyed incarnation is void and none of them may be presented again."
packages/plait/src/internal/registers.ts#2
  kind "incarnation-mismatch"
  law "A fencing token is honored only by the backing-stream incarnation that minted it."
  expected <expression>
  next[0].subject "register.open"
  next[0].note "Re-open the register and re-derive the fence from the live bucket; a reborn bucket's revisions restart, so every token minted under the destroyed incarnation is void and none of them may be presented again."
packages/plait/src/internal/registers.ts#3
  kind "stale-register-token"
  law "renew requires the current fencing token"
  expected <absent>
  next[0].subject "register.observe"
  next[0].note "Observe the register for the current token and holder; this lease is superseded; do not renew with this token."
packages/plait/src/internal/registers.ts#4
  kind "stale-register-token"
  law "no stale token ever lands"
  expected <absent>
  next[0].subject "register.observe"
  next[0].note "Observe the register for the current token and landed outcome; this round is superseded; do not retry this commit."
packages/plait/src/internal/registers.ts#5
  kind "concurrent-register-update"
  law "expire-steal grants a strictly larger token from the current revision"
  expected <absent>
  next[0].subject "register.observe"
  next[0].note "Re-read the register at its current revision and re-attempt the expire-steal against it."
packages/plait/src/internal/sessions.ts#0
  kind "session-transport-unavailable"
  law "Transport absence may be retried; writ and view refusals may not."
  expected "the pinned local NATS KV operation to be available"
  next[0].subject "Session.subscribe"
  next[0].note "Reconnect and subscribe again; a session is a value the substrate never learned, so re-opening under the same writ resumes the same read."
packages/plait/src/internal/substrate.ts#0
  kind "malformed-value"
  law "A substrate session folds the substrate's own declaration, field by field, from the declared roster."
  expected <absent>
  next[0].subject "substrate.declaration"
  next[0].note "Fold the substrate declaration from an established connection's own server information; every declared field carries the sort the substrate sends it with."
packages/plait/src/internal/successors.ts#0
  kind "invalid-fold-state"
  law "Fold states are wire-grammar values whose state digest is SHA-256 over canonical bytes."
  expected "one RFC 8785 wire value"
  next[0].subject "Fold.declare"
  next[0].note "Return a canonical wire value from every contribution and algebra combination."
packages/plait/src/internal/transport.ts#0
  kind <expression>
  law <expression>
  expected <expression>
  next <expression>
packages/plait/src/internal/writs.ts#0
  kind "malformed-value"
  law "A substrate connection acts under a writ this package has declared for its layer."
  expected <absent>
  next[0].subject "substrate.writ"
  next[0].note "Declare this layer's writ as a row of the substrate writ table, naming the carrier roles its connection acts as, before the layer opens a connection."
packages/plait/src/kernel/Subjects.ts#0
  kind "invalid-subject-token"
  law "Fabric routing tokens are non-empty NATS tokens without dots, whitespace, or wildcards."
  expected "one literal NATS subject token"
  next[0].subject "flb.fab.ev.example.0"
  next[0].note "Replace the refused segment with a non-empty literal token such as example."
packages/plait/src/kernel/Wire.ts#0
  kind "malformed-envelope"
  law "Envelope v0 is a closed struct; excess properties are refused." | "Envelope v0 fields must have the fixed wire shapes declared by the fabric contract."
  expected "no excess property" | <expression>
  next[0].subject "decode"
  next[0].note "Submit one closed Envelope v0 value with the expected field shape."
packages/plait/src/kernel/Wire.ts#1
  kind "malformed-envelope"
  law "Envelope v0 fields must have the fixed wire shapes declared by the fabric contract."
  expected "one constrained JSON envelope"
  next[0].subject "decode"
  next[0].note <expression>
packages/plait/src/kernel/Wire.ts#2
  kind "malformed-blob-reference"
  law "Envelope v0 reserves {blob: Digest} as an exact closed body form."
  expected "no excess property" | "exactly one blob field carrying a lowercase SHA-256 digest"
  next[0].subject "decode"
  next[0].note "Replace the body with the exact {blob: Digest} form."
  next[0].body <expression>
packages/plait/src/kernel/Wire.ts#3
  kind "malformed-envelope"
  law "Envelope v0 fields must have the fixed wire shapes declared by the fabric contract."
  expected <expression>
  next <expression>
packages/plait/src/kernel/Wire.ts#4
  kind "inline-body-too-large"
  law "Envelope v0 inlines at most ${} canonical body bytes; larger bodies use {blob}."
  expected <expression>
  next[0].subject "decode"
  next[0].note "Store the body as a blob and submit the exact {blob: Digest} form."
  next[0].body <expression>
packages/plait/src/kernel/Wire.ts#5
  kind "digest-mismatch"
  law "Nats-Msg-Id must equal SHA-256 over the envelope's canonical uncompressed bytes."
  expected <expression>
  next[0].subject "Nats-Msg-Id"
  next[0].note "Re-derive SHA-256 from the canonical uncompressed envelope bytes before publishing."
  next[0].body <expression>
packages/plait/src/planes/Address.ts#0
  kind "invalid-petname"
  law "A petname is one non-empty name carrying no separator and no control character, and is never . or ..: the relative forms name a position instead of a value."
  expected "one literal petname"
  next[0].subject "Address.at"
  next[0].note "Replace the refused segment with a literal name; reach an enclosing directory by starting the walk at its own root digest, never by stepping out of this one."
packages/plait/src/planes/Address.ts#1
  kind "not-a-directory"
  law "Every hop of a path opens a directory value; a walk never reinterprets a value that is not one."
  expected "one directory value"
  next[0].subject "Address.directory"
  next[0].note "Publish a directory under this digest, or shorten the path to the value this hop already names."
packages/plait/src/planes/Address.ts#2
  kind "unbound-petname"
  law "A name resolves only against a directory that binds it, and a root digest names one immutable directory, so the answer never moves."
  expected "one binding for this name in the directory this hop opened"
  next[0].subject "Address.directory"
  next[0].note "Bind the name in a directory value, publish it, and walk from the digest that publication returned."
packages/plait/src/planes/Address.ts#3
  kind "ambiguous-binding"
  law "A name bound to more than one digest resolves to none of them: the directory carries every candidate, and nothing in a walk arbitrates between them."
  expected "exactly one digest bound to this name"
  next[0].subject "Address.directory"
  next[0].note "Publish a directory binding this name to exactly one digest and walk from that root; arbitration between concurrent binders is a fenced register decision and is not read here."
packages/plait/src/planes/Anchor.ts#0
  kind "invalid-anchor-advance"
  law "An anchor records only a contiguous successor frontier; its floor advances by exactly one applied position."
  expected <expression>
  next[0].subject "Folds.deploy"
  next[0].note "Buffer this arrival by position and advance only when the next contiguous successor is present."
packages/plait/src/planes/Blob.ts#0
  kind "blob-absent"
  law "A blob reads only from a store that already holds bytes under its digest."
  expected <expression>
  next[0].subject "blob.put"
  next[0].note "Put the bytes into this store and reference them under the digest it returns."
packages/plait/src/planes/Blob.ts#1
  kind "digest-mismatch"
  law "A blob's identity is re-derived over the fetched bytes and never asserted by its store."
  expected <absent>
  next[0].subject "blob.put"
  next[0].note "Re-put the original bytes: this store is holding bytes that do not hash to the requested digest."
packages/plait/src/planes/Environment.ts#0
  kind "ambiguous-binding"
  law "One hole carries at most one provision per position; ties are the fenced register's to arbitrate, never a read's."
  expected "one provided value at this position"
  next[0].subject "Environment.greatestAt"
  next[0].note "Route the conflicting provisions through a fenced rebind, or provide at distinct positions."
packages/plait/src/planes/Fold.ts#0
  kind "unearned-commutative-algebra"
  law "F4 licenses partitions > 1 only for an algebra whose digest-seeded ACI law suite earned the commutative brand."
  expected "the runtime witness produced by Algebra.commutative after its derived law suite passes"
  next[0].subject "Algebra.commutative"
  next[0].note "Provide a seeded arbitrary for the derived associativity, commutativity, and identity properties, or declare one partition."
packages/plait/src/planes/Lane.ts#0
  kind "invalid-lane-declaration"
  law "A lane is canonical data with one literal route handle, a positive partition count, and a declared key path."
  expected <absent>
  next[0].subject "Lane.declare"
  next[0].note "Declare a literal lane handle, 1..1024 partitions, an admitted event-schema digest, and a key path."
packages/plait/src/planes/Lane.ts#1
  kind "invalid-partition-key"
  law "Lane partition routing is derived only from the declared path over the admitted event value."
  expected <absent>
  next[0].subject "Lane.partitionKey"
  next[0].note "Choose a path present in every admitted event, or use an empty path to key by the whole event."
packages/plait/src/planes/Resolved.ts#0
  kind "cataloged-value-absent"
  law "A reference resolves only against a catalog or payload store that holds its value."
  expected <expression>
  next[0].subject "catalog.publish"
  next[0].note "Publish the value under this digest before resolving the reference, then decode again."
packages/plait/src/planes/Resolved.ts#1
  kind "digest-mismatch"
  law "A resolved value's identity is re-derived on read and never asserted."
  expected <absent>
  next[0].subject "catalog.publish"
  next[0].note "Re-derive the digest from the value's canonical uncompressed bytes and reference it under that digest."
packages/plait/src/planes/Resolved.ts#2
  kind "malformed-value"
  law "Bytes admitted at a digest decode as exactly one RFC 8785 wire value."
  expected "one RFC 8785 wire value"
  next[0].subject "catalog.publish"
  next[0].note "Store the payload as the canonical uncompressed bytes of one RFC 8785 wire value."
packages/plait/src/planes/Session.ts#0
  kind "invalid-session-declaration"
  law "A session declares one holder, a set of declared views, an anchor policy this seam knows, and a partition its fold's lane declares."
  expected <absent>
  next[0].subject "Session.writ"
  next[0].note "Declare a holder, the view digests this reader may image, a resume or replay policy, and a partition the lane declares."
packages/plait/src/planes/Session.ts#1
  kind "undeclared-view"
  law "A session emits only the image of the declared fold it subscribed to, and only while its writ still names that view."
  expected <absent>
  next[0].subject "Session.writ"
  next[0].note "Declare this fold as a view of the writ, or read through the session opened for a view the writ already names."
packages/plait/src/surface/cli.ts#0
  kind "invalid-chaos-request"
  law "plait chaos runs one pinned span of one admitted declared fold; no ambient head or arbitrary program is accepted."
  expected <absent>
  next[0].subject "plait chaos"
  next[0].note <absent>
packages/plait/src/surface/cli.ts#1
  kind <expression>
  law <expression>
  expected <absent>
  next <absent>
packages/plait/src/surface/mcp.ts#0
  kind "malformed-value"
  law "A tool call carries exactly the fields its served input schema declares, in their declared spellings."
  expected <absent>
  next[0].subject "tools.schema.json"
  next[0].note "Follow the served input schema; every field is named for the value it carries."
packages/plait/src/surface/mcp.ts#1
  kind "non-canonical-value"
  law "A value travels as its one canonical byte form; bytes the constrained decoder refuses have no identity to name."
  expected "one RFC 8785 wire value"
  next[0].subject <expression>
  next[0].note "Send the value's canonical uncompressed bytes."
packages/plait/src/surface/mcp.ts#2
  kind "non-canonical-value"
  law "One canonical form exists; excess or non-canonical bytes refuse."
  expected <expression> | "one RFC 8785 canonical form"
  next[0].subject <expression>
  next[0].note "Canonicalize before sending; the bytes are the identity."
packages/plait/src/surface/mcp.ts#3
  kind <expression>
  law <expression>
  expected <expression>
  next <expression>
packages/plait/src/surface/mcp.ts#4
  kind "malformed-value"
  law "The served tool list and the handler record name the same tools; a served tool with no handler is a lie on the wire."
  expected <expression>
  next[0].subject "mcp.kernelHandlers"
  next[0].note "Regenerate the artifact copy and align the handler record with its tool rows."
packages/plait/src/truth/Algebra.ts#0
  kind "invalid-algebra-declaration"
  law "A declared algebra's definition and initial state are canonical wire-grammar values."
  expected <absent>
  next[0].subject "Algebra.declare"
  next[0].note "Declare the reducer with canonical data and a canonical initial state."
packages/plait/src/truth/Algebra.ts#1
  kind "unearned-commutative-algebra"
  law "F4's commutative brand is earned only when digest-seeded, distinct identity, associativity, and commutativity cases pass."
  expected <absent>
  next[0].subject "Algebra.commutative"
  next[0].note "Provide a seeded arbitrary that can derive at least 32 distinct triples before partitioned deployment."
packages/plait/src/truth/Canonical.ts#0
  kind "non-canonical-value"
  law "Canonical bytes exist only for values admitted by foldlab's RFC 8785 seam."
  expected "one RFC 8785 wire value"
  next[0].subject "canonicalize"
  next[0].note "Replace the refused value at the named path with one RFC 8785 wire value."
packages/plait/src/truth/Refusal.ts#0
  kind <absent>
  law <expression>
  expected <expression>
  next <expression>
